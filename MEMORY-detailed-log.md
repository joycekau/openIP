---
name: kol-meme-project
description: "oneIP.io (formerly kol.meme) — Solana creator-IP/KOL token launchpad + gmgn-style terminal; layout, data strategy, what's built"
metadata: 
  node_type: memory
  type: project
  originSessionId: 29cef1d9-6c8e-483e-ac09-ea3dc00ae55e
---

**oneIP.io** (rebranded from kol.meme on 2026-06-27; tagline "tokenize your influence", IP-Fi positioning) = a gmgn.ai-style Solana creator-IP / KOL token launchpad + terminal. Repo folder is still `C:\Users\user\kol-meme` and internal identifiers (KolWallet, KolI18n, /api/kol, kol-wallets.json) keep the "KOL" feature name — only the user-facing brand string changed. User wants to "surpass" gmgn.ai and rise.rich, "protect merchants and ordinary users," tokens "can't go to zero," and a "noble/luxurious" (高贵) dark+gold UI.

**FINAL core mechanic "constitution" (locked 2026-06-28, user confirmed direction A = open pool):** (1) **Buy** splits 80% → bonding-curve LP / 20% → floor-vault PDA (no withdraw path). (2) **Sell** routes a **2% sell tax INTO the floor vault** (dumping raises the floor for remaining holders) — replaces the rejected "can only sell 90%" idea (bypassable/useless). (3) **Redeem anytime**: any holder burns tokens for pro-rata share of the vault → mathematical floor, whales can't break it → "can't go to zero" + "崩了能按持仓比例拿回钱" without any halt. (4) **Anti-rug**: mint authority = program PDA, **NO freeze authority** (not a honeypot), creator can't pull liquidity. (5) **Open**: standard SPL + Metaplex metadata, tradable on Raydium/Jupiter, credible in every wallet. **REJECTED by user after honest pushback:** auto circuit-breaker halt at −70%/24h (needs transfer-freeze = honeypot flag + closed pool + false-triggers on normal memecoin volatility) and the **10% platform-frozen-180-day** (centralized custody = trust/regulatory hole; the 20% vault already IS the insurance). Open implication to resolve at code time: 80/20 split + 2% sell tax are enforced by oneIP's own buy/sell instructions; trades directly on Raydium bypass them unless using Token-2022 transfer-fee — likely model = trade on the bonding curve, the floor vault + redemption persist after any graduation.

**Step-2 economics (locked 2026-06-28):** the 20% floor **belongs to holders** — neither platform nor creator can withdraw it (user confirmed "创币者国库" functionally = the holder protection pad). **Creator revenue (3 fair sources, user approved):** (1) holding tokens they bought fairly (NO premint/presale); (2) off-chain Pro $10/mo + fan subs (already built); (3) a **0.50% cut of every trade**. Implemented in `lib.rs`: the 1.25% trade fee is now split `PLATFORM_FEE_BPS` 0.75% / `CREATOR_FEE_BPS` 0.50% (taken from the platform fee, NOT carved from the 2% floor tax, so the floor stays full-strength; creator earns on buy+sell volume not just dumps). Added `creator` account (`address = launch.creator`) to Buy + SellMarket. **Step-2 finished 2026-06-28 (all user-confirmed):** (a) **graduation = YES** — when curve fills `GRADUATE_LAMPORTS` ~85◎, permissionless `graduate()` flips `graduated`, stops new curve buys (`require!(!graduated)` in buy), trading moves to Raydium; floor vault + redeem persist forever (Raydium migration CPI is a documented reference stub). (b) **launch fee = 0.1◎** (`LAUNCH_FEE_LAMPORTS`) paid to fee_recipient in `create_token`; added `config` + `fee_recipient` (has_one) to CreateToken. (c) Added `graduated: bool` to TokenLaunch (+ Graduate accounts struct, KolErr Graduated/AlreadyGraduated/BelowGraduationThreshold). kolpad now has 8 instructions / 8 account structs. STILL DEFAULT (tune later): curve params VIRTUAL_SOL 2◎ / INIT_TOKENS 1e9. NOTE: the off-chain launchpad demo does NOT charge the 0.1◎ fee (placeholder); the fee is real only on the deployed chain program. **AiToEarn multi-platform publishing (white-labeled, hidden behind oneIP):** `src/publish.js` = thin zero-dep wrapper of AiToEarn REST v2 (connectStart/connectStatus/listAccounts/publish/publishStatus), 14 platforms, mock mode when no `AITOEARN_API_KEY`. server.js exposes 5 routes (`/api/publish/connect`, `/connect/status`, `/accounts`, `/now`, `/status`) all gated on `creator.pro` (returns 402 "Pro ($39/mo) required"). Bound account ids persist on `creator.publishAccounts`. **Frontend now built (2026-06-28):** feed.html Creator Studio has a "Multi-platform publishing" panel (Pro: platform connect chips via `loadPublish()`/`pubConnect()` with OAuth-popup + status polling; non-Pro: $39/mo upgrade prompt), plus a composer "Also publish to my connected platforms" fan-out checkbox wired into `submitPost` → `/api/publish/now`. `.env.example` has `AITOEARN_API_KEY` + `AITOEARN_BASE_URL` slots. Mock flow smoke-tested end-to-end. **PRICING — tiers now split (decided B, 2026-06-28):** `social.js` has `proTier` = "" | "ai" ($10, AI clone) | "publish" ($39, multi-platform, ⊇ ai); helpers `hasAi()`/`hasPublish()`; `setTier()`; legacy `setPro(w,true)`→"ai". `billing.js` takes a `tier` arg, two prices `STRIPE_PRICE_ID` ($10) + `STRIPE_PRICE_ID_PUBLISH` ($39), webhook returns tier. server.js: checkout/dev-upgrade pass tier (dev-upgrade defaults to "publish"), publish routes gate on `hasPublish`, AI auto-reply gates on `hasAi`. feed.html Studio shows tier badge (PRO $10 / PRO+ $39) + separate upgrade buttons `upgradePro('ai'|'publish')`. Tested live: $10 unlocks AI but publishing→402; $39 unlocks publishing. Real Twitter/X go-live needs: user registers own X dev app → creds into AiToEarn config.yaml → set the two env vars (mock auto-switches to real). **AI draft → publish (built 2026-06-28):** `ai.generatePost(creator, prompt)` (Claude, same lazy SDK) drafts a platform-neutral post in the creator's voice; route `POST /api/ai/draft` gated on `hasAi` ($10), mock fallback without a key; feed.html composer has a "✨ AI draft" button (shown for AI-tier) that fills the post box → creator reviews → Post (+ optional fan-out to all platforms). Human-in-the-loop. Possible later: image/video generation, or fully-scheduled auto-posting.

Two related projects on disk (Windows, Node, zero external deps where possible):
- `C:\Users\user\kol-meme` — the data terminal. Built & tested: `npm run feed` (live trending via free DexScreener), `npm run kol` (FIFO PnL leaderboard + signals, replay mode), `npm run serve` (HTTP API + UI), `npm run smoke` (e2e webhook proof). UI at `web/index.html` (dark+gold, fetches live).
- `C:\Users\user\floorpad` — anti-rug floor-backed launchpad (Hardhat, Solidity). 20% locked treasury (vs rise.rich ~5%), no withdraw path for anyone, floor only ratchets up. 6 tests pass.

Data strategy (README has full map): **Path A** = call gmgn OpenAPI directly (`gmgn.ai/ai` key, 6 skills, fastest). **Path B** = self-index from primary sources (DexScreener free, Birdeye, Helius, Bitquery) — independent, the moat. Adapters stubbed in `src/sources/`.

KOL-tracking core = labeled KOL wallet DB → Helius webhooks on those wallets → decode swap → FIFO PnL engine (`src/kol/pnl.js`) → leaderboard + live signal feed. **NOW LIVE**: user added a working `HELIUS_API_KEY` to `.env`; `npm run kol` runs in LIVE (Helius) mode on real on-chain data. `npm run discover` pulls real active trader wallets from the hottest token's swaps (gmgn methodology). `data/kol-wallets.json` is seeded with 4 real discovered (unvetted) wallets — replace with real WebTVAsia/KOL addresses. Fixed parseSwap in `src/sources/helius.js` to decode tokenTransfers/nativeTransfers (events.swap is empty on this tier).

Also built in kol-meme: a **creator social layer** (`src/social.js`, page `/feed` = `web/feed.html`) — creators post products/images, fans follow/subscribe/comment, plus a "Creator Studio" with keyword→reply canned auto-replies ("一般的回复") and a `pro` flag. $10/mo Pro tier AI clone auto-reply is **built** (`src/ai.js`, official `@anthropic-ai/sdk` via lazy dynamic import, default model `claude-opus-4-8`, override `AI_MODEL`): fires in `social.addComment` when no canned rule matches AND creator is `pro` + `aiEnabled`. To enable: `npm install @anthropic-ai/sdk` + set `ANTHROPIC_API_KEY`. Creator Studio (feed.html) has the AI toggle + persona box for Pro creators. $10/mo Pro is **self-serve** via Stripe (`src/billing.js`): `/api/pro/checkout` → Stripe Checkout (live) or dev mode (instant grant for local testing); `/webhooks/stripe` with HMAC signature verification sets `pro`; "Upgrade to Pro" button in feed.html Creator Studio. Live needs STRIPE_SECRET_KEY + STRIPE_PRICE_ID ($10/mo recurring) + STRIPE_WEBHOOK_SECRET + PUBLIC_URL. Social-platform sync is **scaffolded** (`src/sync.js`): creator binds `syncPlatform` + `syncHandle` in Creator Studio, `POST /api/sync/now` mirrors their latest posts into the feed (marked `synced`+`source`, shown with a ↻ badge). Mock mode works now; real mode = wire `fetchLatest()` per platform with the creator's OAuth read token. Optional prod scheduler not yet added. An **admin panel** at `/admin` (`web/admin.html`, token-gated by `ADMIN_TOKEN` env, default "admin") to manage tracked KOL wallets (CRUD via `src/wallets.js`, the single source of truth for `data/kol-wallets.json`) and verify/unverify launches; RugCheck security badges on the feed (`src/security.js`), and the **social-verified launchpad** (`src/launchpad.js` + `src/verify.js` + `npm run launch:smoke`): bind ≥3 socials → centralized attester signs → pushed on the board; <3 socials → tradable but not boosted; every launch has 20% floor.

**Key product decisions (user-chosen):** chain = **Solana (Rust/Anchor)**; social verification = **centralized signed attestation** (ship now, can move to zkTLS later); platform "kol coin" = **tradable but with 20% floor**; onboarding = **non-custodial Phantom wallet** (`web/wallet.js`, integrated into both pages); wallet = users hold own keys, no fiat/custody.

**Go-to-market (cold start):** first KOLs from **WebTVAsia** (SEA YouTube MCN) creator network. **Dropped China/Weibo** (crypto illegal in mainland) — **focus Southeast Asia first**. UI is localized 7 languages (`web/i18n.js`): EN / 中文 / ไทย / Bahasa / Tiếng Việt / 日本語 / 한국어, with a header language switcher. Helius has a **free $0 tier** (1 webhook can watch many wallets) — no need to pay for MVP; everything else (DexScreener, RugCheck, OAuth, Phantom) is free too. Biggest funnel risk (user accepts it): entertainment fans aren't crypto-native, non-custodial Phantom has high drop-off; can add embedded wallet later. Real established creators make the 3-social verification meaningful — next: follower-gated OAuth so the badge isn't fakeable. The on-chain trustless program is at `C:\Users\user\kolpad` (Anchor; `programs/kolpad/src/lib.rs` + pure `curve/` crate with runnable `cargo test`). No Rust toolchain on this machine — Anchor needs WSL on Windows. floorpad (Solidity) is now superseded by kolpad (Solana) for the launch contracts. **Cross-wallet credibility (logos in all wallets) = Metaplex Token Metadata standard**: `create_token` now takes a `uri` + CPIs Metaplex `create_metadata_accounts_v3` (logo/name/site), with fair authorities (mint=launch PDA, no freeze). Off-chain ready: launch form collects logo/website/socials, `GET /api/coin/metadata` builds the standard JSON, `src/ipfs.js` (Pinata, `PINATA_JWT`) + `POST /api/coin/finalize` pin logo+JSON to IPFS for a permanent `uri`. Real cross-wallet display needs: deploy kolpad + a Pinata key.

**Outbound multi-platform publishing = AiToEarn as a hidden white-label engine (decided & built 2026-06-28).** AiToEarn (https://github.com/yikart/AiToEarn, MIT) cloned to `C:\Users\user\AiToEarn`; full Docker stack runs locally (nginx 8080 + web/server/ai + mongo/redis/rustfs, all healthy). It's a heavy NestJS+nx+Mongo+Redis monorepo — **DO NOT merge its source into kol-meme** (would wreck oneIP's zero-dep lightweight arch). Use it headless as a publish engine. REST API v2, auth header **`x-api-key`**, base `…/api/v2`. Key endpoints: `POST /channels/publish/flows` (one call → all-platform publish; body `{content:{title?,body?,media:[{url,options}],cover?}, publishAt, items:[{accountId,platform,overrides?}]}`), `GET /channels/publish/flows/:flowId`, `GET /channels/accounts`, `GET /channels/accounts/auth/:platform?redirectUri=…` (returns platform auth URL — **accepts oneIP's own redirectUri**) + `…/status/:sessionId` (poll). Also 3 MCP servers + AI-gen tools (createVideoDraft/createImageTextDraft). Each platform config (twitter.config.ts) takes OWN clientId/clientSecret/redirectUri/logoUrl; Relay optional → **fully white-label: creator only sees oneip.io → platform's own consent page (says "oneIP" with own dev app) → oneip.io; AiToEarn never appears.** Relay = fast for testing (consent page shows AiToEarn briefly); swap to own per-platform dev apps before launch. Local fix: removed redis 6379 host mapping in compose (conflicts with user's quantdinger-redis).

**Built in oneIP (`C:\Users\user\kol-meme`):** `src/publish.js` thin adapter (connectStart/connectStatus/listAccounts/publish/publishStatus, mock mode when no `AITOEARN_API_KEY`, env: `AITOEARN_BASE_URL` default http://localhost:8080/api, `PUBLIC_URL`) + 5 routes in server.js, all Pro-gated (402 if not pro): `POST /api/publish/connect`, `GET /api/publish/connect/status` (persists bound accountId onto creator.publishAccounts), `GET /api/publish/accounts`, `POST /api/publish/now`, `GET /api/publish/status`. Per-creator account ids tracked oneIP-side under one platform master key. This fills the OUTBOUND gap `src/sync.js` (inbound mirror) left. Full flow smoke-tested in mock mode ✓. **Pro tier price raised $10 → $39/mo** (auto-generate + all-platform publish is the Pro hook). **Next:** wire Twitter/X end-to-end for real (register own X dev app, set AITOEARN_API_KEY + creds in AiToEarn config.yaml); add a publish UI to feed.html Creator Studio; then AI-generate via the draft MCP tools.

**🚀 kolpad DEPLOYED to Solana devnet (2026-06-28):** Program Id `5SVSaKceFdzdynnuGJDFy74tspXDDxJccWRXh6B1NUdG` (declare_id + Anchor.toml updated to match). Built & deployed via **Docker** (no WSL needed): `backpackapp/build:v0.30.1` image + a named cargo volume + `kolpad-solana` volume holding the devnet deployer wallet `4U5Jzb2H6DcmwqaNEMibBDy1zhezWutsjJXE1ByuC17r` (= upgrade authority). The `curve/` math is proven (`cargo test` 5/5 inc. sell_tax_raises_floor). **Toolchain battle won:** Anchor 0.30.1 image's old Cargo(1.79)/rustc(1.75) hit the 2025 edition2024 + MSRV crate wave; fixed by pinning in `onchain/Cargo.lock` (blake3→1.5.5, zeroize_derive→1.4.2, bytemuck→1.21/derive→1.8.1, proc-macro-crate→3.2.0, jobserver→0.1.31, indexmap→2.6.0, borsh→1.5.x) + MSRV-aware `cargo update` (CARGO_RESOLVER_INCOMPATIBLE_RUST_VERSIONS=fallback, rust-version="1.75" in Cargo.toml) for unicode-segmentation etc. + lockfile must be **version 3** (old cargo). `anchor build --no-idl` to skip the ark-bn254 `MontFp!` IDL-gen panic (program .so builds fine; IDL still broken). Deploy cost ~2.77◎ rent for the 397KB program; devnet airdrop is rate-limited (CLI/Helius blocked) — user got SOL from faucet.solana.com after forking public repos (it requires ≥3 public repos). **STILL TODO on-chain:** call `initialize` (set Config attester+fee_recipient) then `create_token`/`buy` to prove instructions end-to-end — needs a client (IDL broken, so either fix IDL or hand-roll a web3.js client); wire off-chain launchpad.js to the real program; later mainnet. Re-deploy/upgrade: `solana program deploy target/deploy/kolpad.so --program-id target/deploy/kolpad-keypair.json` with the kolpad-solana volume.

**🎯 ON-CHAIN INSTRUCTIONS PROVEN end-to-end on devnet (2026-06-28):** Built a hand-rolled web3.js client (NO IDL needed — IDL still broken) in `scripts/sol/` (`lib.js` = discriminator `sha256("global:<name>")[0..8]` + PDA/metadata-PDA derivation + borsh string/u64 + loads `onchain/.deployer.json`; `01-initialize.js`, `02-create-token.js`, `03-buy.js`; npm scripts `chain:init/create/buy`). Ran all three on devnet with the deployer wallet: **initialize** → Config PDA `6nL6MtAzcPnpYuGJ27H5DQ8X6r16FBvxyhMpShCBiSqg` (attester+fee_recipient = deployer). **create_token** → real SPL mint `9foiwALjR6Dkh2661HrWrQMobEryieZPqa5EVMtU5Dkh` + launch PDA + floor vault + Metaplex metadata + 0.1◎ launch fee, fair authorities. **buy 0.5◎** → floor vault got **exactly 0.098750◎ = 20% of net (after 1.25% fee)**, buyer got ~164.9M tokens — THE 20% FLOOR IS PROVEN ON-CHAIN, publicly verifiable. Deployer key extracted from the `kolpad-solana` docker volume to `onchain/.deployer.json` (gitignored). @solana/web3.js + spl-token are **devDependencies** (only the scripts use them; server is still zero-dep; Render builds with `npm install --omit=dev --omit=optional`). The "bigint failed to load bindings" warning from spl-token is harmless. **NEXT:** wire these instructions into the BROWSER (Phantom signs) so the `/launch` page actually mints real on-chain tokens (account ordering for each ix is now known from the working scripts — port them to client-side web3.js loaded via CDN); replace the off-chain placeholder launchpad. Then: sell_market/redeem_floor/verify_token/graduate clients; immutable metadata + revoke mint at graduation; Jupiter Verify; mainnet.

**Brand redesign (2026-06-28):** user supplied a new brand kit — vibrant gradient (cyan→violet→magenta→coral) on dark purple-black, chat-bubble smiley logo, mascot **Onee** (white robot/cat-hoodie 3D char). Replaced the old dark+gold "noble" theme across ALL pages (home/terminal/launch/feed/coin/admin) via hex-swap of the shared palette + SVG gradient logo (reads "oneIP.io") + gradient buttons/avatars/badges. Home hero headline = "把影响力，铸成一枚永不归零的币" + taglines "网红IP发币·连接粉丝·价值共创 / Create Value·Connect Fans·Earn Together" + 4 pills (发币/社交/共创/激励). Mascot loads from `web/assets/onee.png` (user must save the image; SVG bubble is the onerror fallback); server has an `/assets/*` static route. Fonts: Plus Jakarta Sans + Noto Sans SC + Space Grotesk. **Flap.sh-style credibility (user researched, agreed to match):** 2 layers — (1) Metaplex metadata + immutable + revoked authorities = logo shows in all wallets + "fair" signal; (2) Jupiter Verify = blue check + network-wide visibility (auto-discovered once real liquidity/organic score, or paid express ~1000 JUP). kolpad already does Metaplex metadata + no-freeze + PDA-mint; TODO: make metadata immutable + revoke mint authority at graduation; Jupiter Verify needs the Raydium graduation + traction. Pinata JWT verified working (cross-wallet metadata pinning ready).

**🟡 商业闭环方向（CONSIDERING / 未锁定，2026-06-28）—— 给币加真实现金流，破 pump.fun「人人发币但无价值」死穴。** 设想：网红从对接的**中国供应链一件代发(dropshipping)货源**选品（网红零库存，供应商发货）→ oneIP 一键生成商品落地页挂在其币页 → 粉丝下单付款 → 供应商发货 → **订单结算（过退款期、利润落定）后，净利润 ×20% 由结算服务触发链上一笔 → 灌入该币的 floor vault**（卖一单货=抬高所有持币人的数学保底），链上可查。核心不是用哪个商城，而是那台「结算→回购引擎」；商城层可替换。**明确决定：不要把 CRMEB(PHP/ThinkPHP) 或任何重型商城源码套进 oneIP**（与拒绝合并 AiToEarn 源码同理，会毁零依赖轻架构）——只在现有 Node 栈写薄薄的「商品-订单-结算」模块 + 货源用现成 API 适配器接（候选：1688分销 / 国内代发 SaaS / 用户自有供应商，**货源未定**）。设计倾向：20% **直接灌 floor vault**（零滑点、不可抢跑、精确抬地板、最贴合"永不归零"品牌），而非市场买入拉盘（留作可选）。**🔒 法币铁律（user 强调"必须"，2026-06-28）：商品买卖一律法币结算（Stripe + 东南亚本地支付/电子钱包：GCash/GrabPay/Maya/DANA/ShopeePay 等），粉丝绝不用代币买货**（契合记忆里"娱乐粉丝非币圈、Phantom 高流失"的漏斗风险——买货零加密门槛）。**唯一的加密动作在协议侧**：平台收法币 → 结算 → 把净利润 20% 换成 SOL → 灌 floor vault。需 fiat→SOL 兑换路径，建议**平台预备 SOL 浮动池**（buyback 即时从浮动池灌地板，平台再批量把法币利润经交易所/OTC 兑 SOL 回补浮动池，解耦"粉丝侧即时性"与"兑换批处理"）。**新增合规考量**：平台托管法币 + 法币兑加密 = 可能触及货币传输/资金牌照，东南亚按国别要查清。**对外措辞铁律（user 强调）：只讲「营收→协议回购→抬高保底」(utility/协议机制)，绝不讲「分红/上市/持币人分钱」——后者触发证券(Howey)红线。**「类似企业上市」只是内部描述闭环用，不对外。**风险（须正视）：**(1) 证券/监管——措辞如上；(2) 履约——东南亚+中国供应链物流/退货/客服，烂货会同时砸网红的币和人设，需选品质控门槛；(3)「利润」定义死 + 回购**只对已结算、过退款期的净利润触发**，否则退款致地板虚高引发信任崩塌。**落地节奏：先做 Phase 1 薄切片**（一个货源 API + 商品落地页 + checkout + 结算→链上灌地板），用 1 个网红 + 几个 SKU 跑通「卖一单→链上看到地板涨」闭环再谈规模化。主攻**东南亚**（与主项目一致，已砍中国大陆）。

**✅ Phase 1 后端已建并跑通（2026-06-28, mock+HTTP 全绿）：** 新 `src/commerce/` 五模块（零依赖、同 social.js/publish.js 风格、各自 `data/*.json` 持久化）：`source.js`（货源 adapter，Phase1=manual 手录 products.json，1688/SaaS 之后换实现接口不变，mock 履约）、`orders.js`（订单状态机 pending→paid→shipped→delivered→**SETTLED**/refunded，净利=price−cost−fee，FEE_BPS 290+30¢，REFUND_WINDOW_DAYS 7；**铁律：buyback 只在 settle 且过退款窗口触发，settle 幂等，已结算不可退款**）、`treasury.js`（SOL 浮动池会计，`SOL_USD_PRICE` 默认150，topUp/reserve，fiat→SOL 兑换 out-of-band stub）、`buyback.js`（BUYBACK_BPS 2000=20%，reserve 浮动池→灌地板→存证 per-coin proof；on-chain **lazy+opt-in** `SOLANA_BUYBACK=1` 时动态 import `scripts/sol/buyback.js`，否则 mock）、`payments.js`（Stripe 一次性 `payment` 模式 + 东南亚本地法 `STRIPE_PAYMENT_METHODS`，unkeyed=devMode，webhook 验签同 billing.js）。链上 `scripts/sol/buyback.js` 导出 `fundFloorViaBuy({mint,lamports})`=复用已证 `buy` 指令(20%自动落 floor vault)，只在真实模式被 import。server.js 接了路由：公开 `GET/POST /api/shop/{products,product,order,proof}` + `GET /api/shop/order` + `POST /webhooks/shop` + `POST /api/shop/dev-paid`；admin（x-admin-token）`/api/admin/shop/{orders,ship,deliver,refund,settle,treasury,treasury/topup}`。`npm run commerce:smoke`（`scripts/commerce-smoke.js`，fast-exit 不落盘）证全闭环+护栏（窗口前 settle 被挡/退款不灌地板/幂等）。HTTP 端到端也跑通（买$25货→净利$15.97→20%=$3.19→0.0213 SOL 灌地板→proof 可查），测试 data 文件已清。**Phase 1 还差（TODO）：** 前端 storefront（币页/feed Studio 挂商品+「Revenue Floor」展示块，文案只讲营收→回购→保底）；真实货源 API；真实 on-chain（设 `SOLANA_BUYBACK=1`+真 mint，注意 launchpad 当前 mint 是占位符 `Kol...`，需用 chain:create 的真 SPL mint）；真实 fiat→SOL 兑换回补浮动池；退款窗口到期自动 settle 的 scheduler。

**🌐 LIVE + latest UI/terminal state (2026-06-28 eve):** Site deployed → **https://oneip.io** (Render free tier, service `oneip-cegu.onrender.com`, Blueprint from `render.yaml`; build `npm install --omit=dev --omit=optional`, start `node src/server.js`, server binds `0.0.0.0`). GitHub **Bossses001/oneIP.io** (PRIVATE), branch **main** = what Render deploys. GoDaddy DNS: A `@`→216.24.57.1, CNAME `www`→oneip-cegu.onrender.com (HTTPS live). Static `/assets/*` route, cache-control max-age=60. **Terminal (web/index.html) = 3 panels:** ① oneIP 榜 ② 全网 SOL Meme (`src/sources/markets.js` aggregates GeckoTerminal trending+new + DexScreener boosted + pump.fun → `/api/markets`, `/api/markets/{new,pump,oneip}`; 4 tabs 热门/新币/pump.fun/涨幅; 20s auto-refresh) ③ 聪明钱/KOL. Click a coin → on-platform detail modal (`openCoin`: DexScreener chart iframe + stats + Buy-on-Jupiter + RugCheck badge). **Home:** mascot at `web/assets/onee.png` (cropped+flood-fill-transparent from the user's brand board); bubble-logo removed → plain "oneIP.io" text on all pages; hero button 「创业者发币流程」 → `openFlow()` modal showing full 100% allocation (token 100% buyers/0 premint; buy 79% pool/19.75% floor/0.75% platform/0.5% creator; sell 96.75%/2% floor; redeem/launch-fee/graduation/commerce-buyback; creator earnings), bilingual. **Fee kept 1.25%** (0.75/0.5) — user confirmed not changing. **⚠️ MULTI-SESSION (user runs several Claude sessions on this repo): option A — THIS/the active git session owns all code+git+deploy on `main`; other sessions advisory only (don't edit/commit). `dev` branch deleted. If parallel uncommitted changes appear, node --check then commit to main.** **NEXT options:** wire on-chain create_token/buy into /launch via browser Phantom (account-orders proven in `scripts/sol/`); finish commerce frontend (storefront + Revenue-Floor block, see line-42 note); terminal enhancements.

**🟢 双域名架构（user 决定，2026-06-28）：oneIP.ai = 面向全世界普通人/粉丝的「正门」=纯法币电商 + 网红信息 + 粉丝互动 + 全社交分发,完全不露任何币的信息;oneIP.io = 代币引擎（发币/地板/终端）。** 两边后端共用（现仍同一 kol-meme 代码库/服务,.ai 调同一套 `src/commerce/` API）。唯一的代币触点 = .ai 上「查看价值池/Value pool →」这类按钮深链跳去 `.io` 的 `/coin?mint=`。用户在 .ai 注册供应商/网红账号即自动在 .io 生成其 IP 代币;每单 100% 利润的 20% 自动回流该币（已说明的闭环）。拆分理由：供应商上架 + 买家购买都是法币,.ai 就是「经营传统电商生意」,信用卡付款、电商履约/快递,只是多了「自动生成代币 + 20%回购」这一后台流程。**对外仍守铁律：.ai 上只讲营收→价值池（utility/链上机制、"not a payout"）,绝不出现 token/coin/分红 字样。**

**🔼 oneIP.ai 范围扩展（user 补充,2026-06-28）：oneIP.ai 不止电商,是「创作者全能变现正门」。** 创作者可发布并变现:实物产品、自己的照片/视频/演艺/创作作品、付费解锁/体验、NFT、或开放粉丝打赏(tip);创作者自己定价或让粉丝自由打赏。**铁律不变且横跨所有类型:任何产生的净利润 20% 一律自动回流 oneIP.io 买该创作者代币、注入地板。** 设计=统一「变现条目类型」`type ∈ {physical, digital, tip, nft}`,共用同一台 type-agnostic 的 settle→20%→buyback 引擎(`buyback.js`/`treasury.js` 已经只认 coin+净利润,无需重做);差异只在「成本算法 + 交付 + 结算速度」:physical=售价−供货−费/供应商发货/收货+退款窗口(已建);digital=售价−费(成本≈0)/即时解锁下载/付款即结算;tip=金额−费/无交付/即时不可退;nft=售价−费−铸造费/Metaplex 铸到粉丝钱包/即时(最重,放后面)。注意:tip/digital 净利≈全额减费→创作者拿~80%、~20%进币(user 要确认)。**对外措辞铁律延续:.ai 全程只讲「价值池/链上机制」,NFT 按「收藏/会员权益」叙事,绝不 token/coin/分红/投资。** 建议落地序:先把 .ai 变现类型扩成多态(physical 已建→加 tip+digital,复用已建 settle→buyback,工作量小见效快),nft + 网页 Phantom 发币随后。**✅ tip + digital 已建并 preview 验证(2026-06-28）：** `source.js` 加 `type∈{physical,digital,nft}` + `contentUrl`(digital 的付费内容)+ `publicProduct()`(列表/公开接口剥离 contentUrl,防泄露,已验证 listing 不含 contentUrl);`orders.js` 加 `isInstant(type)=type!=='physical'`、`createTip({creator,coin,amountCents})`(无 catalog,net=金额−费)、`publicOrder()`(未解锁前不露 deliverPayload)、`markPaid` 分叉(physical→供应商发货;instant=digital/tip/nft→即时 deliver+settle,digital 解锁 content);`buyback.execute` 加**队列兜底**:float 不足时 settle 仍成功、buyback 记为 `status:"queued"`(等平台下次 fiat→SOL 充池),proof 区分 funded(计入地板)vs queued。server.js 加 `POST /api/tip`;product/order 响应走 publicProduct/publicOrder。前端 ai.html:商品卡按 type 显示徽章(实物/视频/NFT)+ 按钮(digital=🔓解锁)、digital 购买跳过收货地址且付款后显示解锁内容链接、创作者 rail 每人加「💝 Tip」按钮 → tip modal(金额 chips $1/5/20/50+自定义)→ 付款成功页,全 EN/中文。`npm run commerce:smoke` 扩到覆盖 physical+digital+tip+队列兜底(全绿)。preview 实测:digital 无地址栏+解锁出 contentUrl、tip 成功、buyback 充池后 funded(Aurora +0.0113◎/Nova +0.0061◎),0 console error。**注意:`npm run serve` 经 preview 启动会加载 .env,ADMIN_TOKEN=Qwer1234@admin(非默认 "admin");手动 `node src/server.js` 不加载 .env 则默认 admin。** preview 面板每次 screenshot/重启会重置到 `/`,验证 .ai 要先 `location.assign('/ai')` 再操作。data/ 现有 5 个 demo 商品(4 实物+1 digital, AuroraTV/NovaPlays)+ 测试订单/buyback,可删。**⚠️ preview_screenshot 在 ai.html 上会 30s 超时(打开 drawer/backdrop-filter 后必现,reload 也不恢复)——是 capture 端卡 fixed/blur 图层,不是页面 bug(eval 验证 readyState=complete、元素都在、0 console error);验证 .ai 用 preview_eval 查 DOM,别依赖截图。**

**✅ .ai 完善第二轮（2026-06-28, DOM 验证全过）：** (1) **商城筛选 tabs**(全部/实物/视频,`setFilter()` 客户端按 `normType(p.type)` 过滤,legacy 无 type 当 physical;实测 digital=1/physical=4/all=5)。(2) **创作者主页 drawer**(右侧滑入 `#creDrawer` + `.dr-ov` 背景):点商品卡创作者名 或 创作者 rail 头像/名 → `openCreator(creator,coin)` 拉 `GET /api/creator?w=`(displayName/bio/followers/posts)+ `GET /api/shop/products?creator=` → 渲染头像/名/粉丝数/在售数 + bio + 操作(💝Tip / 价值池深链)+ 在售商品 mini-grid(复用 `cardHtml()`)+ 动态 posts(text/image/❤/💬)。复用了已有 social.js 的 creator/posts(`/feed` 那套数据)。seed 了 AuroraTV(displayName"Aurora TV"+bio+2 posts)/NovaPlays(+1 post)demo 资料。全 EN/中文 i18n。**✅ .ai 完善第三轮 — 创作者 Studio 入驻/选品后台已建（2026-06-28, HTTP+UI 验证全过）：** 新页 `web/studio.html`(路由 `/studio`、`/studio.html`,server.js 已加)= 自助后台,品牌套件一致,EN/中文。流程:**登录**=输入 creator handle 存 localStorage(`oneip_creator`,MVP 用 handle 当身份,注产品级 auth 后补)→ **dashboard**:(1)资料编辑(displayName/bio→`POST /api/creator`);(2)价值池:粘贴 coin mint(存 localStorage `oneip_coin`)+「去 oneIP.io 发币」深链 + 显示该 coin 的 proof(灌入 SOL/成交数);(3)上架表单:类型切换 seg(实物显示供货成本 / 数字显示 contentUrl)→`POST /api/shop/product`;(4)我的上架:列表 + 在售/隐藏 toggle。后端配套:`buyback.execute` 改为**无 coin 优雅跳过**(`{skipped, reason:"creator has no value pool (token) yet"}` 而非 throw)——新人没发币也能先卖货,settle 照常、内容照解锁、20% 暂不路由(TODO:escrow 预发行的 20%,发币后补灌);新增 `POST /api/shop/product/active`(owner-guarded:`p.creator!==creator`→403,实测拦截 Hacker);`GET /api/shop/products` 加 `activeOnly` query(`=false` 给 Studio 看隐藏品,默认 true 公开店面只显在售)。ai.html 的「Start selling/Become a creator/List your product」三个 CTA 改指 `/studio`(Community 仍指 /feed)。preview 实测:登录→dashboard、存资料、类型切换(数字隐藏成本/显示内容栏)、发布数字内容→进列表、toggle 隐藏、owner guard 403、无 coin 商品可卖+settle+解锁+buyback skipped 全过。`commerce:smoke` 加了无-coin case 并**改用每run唯一 COIN**(`"SmokeCoin"+Date.now()`)隔离——因为 `buyback.load()` 读共享 `data/buybacks.json`,跑过的 live server 会污染绝对计数;现全绿。**✅ 网页 Phantom 发币已建（2026-06-28, 字节级 cross-check 通过；真链上签名待真钱包）：** 新 `web/chain.js`(ES module,CDN 引 `@solana/web3.js@1.95.3?bundle` + `buffer@6.0.3`,`globalThis.Buffer` shim)= 浏览器版 kolpad 客户端,把 `scripts/sol/02-create-token.js` 的 account 顺序原样移植:`disc()` 用 WebCrypto sha256("global:create_token")[0..8]、`borshString` u32le+utf8、PDA `findProgramAddressSync`。`OneChain.createToken({name,symbol})`:生成 mint Keypair→算 launch/vault/config/metadata PDA→`feeRecipient=FEE_RECIPIENT`(=Config.fee_recipient=deployer `4U5Jzb2H6DcmwqaNEMibBDy1zhezWutsjJXE1ByuC17r`,**不是 creator**)→uri 默认 `${origin}/api/coin/metadata?mint=`→建 11-key ix→`tx.partialSign(mint)`+Phantom `signAndSendTransaction`(creator 签 feePayer)→confirm→返回 {mint,sig,explorer}。server.js 把 `/chain.js` 加进静态 JS 路由(原只服务 wallet.js/i18n.js)。`launchpad.create` 加可选 `mint`+`mintTx`(真链上传真 mint,否则回退占位 `Kol...`)+ `onchain` 标记。`launch.html` 加 `<script type=module src=chain.js>`,createBtn 改为:连 Phantom→`OneChain.createToken`(按钮显「Minting on Solana…」)→成功后 `POST /api/launch {mint:真, mintTx:sig}`→显示 view tx/mint explorer 链接 + 20% floor;加 devnet 提示 + faucet 链接。**验证:** preview 里 `OneChain.derive('9foiwALj…')` 与 Node `lib.js` **逐字节相同**(disc=`5434cce4188cea4b`、launch=`DUu3bjNxMsGssCJFTEfn8u5acj4pPgKYQHCcfwNYeM4F`、vault=`96vuVUvc…`、config=`6nL6Mt…`、metadata=`9gXMC9x8…`),module 加载 0 error,devnet note + metadata 路由(404 for unknown)都对。**唯一没法在 headless preview 验的=真 Phantom signAndSendTransaction 上链**(需真钱包扩展 + devnet SOL);但字节编码已证同、account 顺序是已在 devnet 成功过的脚本逐字搬运,置信度高。`.ai` Studio 的「去 oneIP.io 发币」深链现在通向真实 mint。chain.js 留了 `OneChain.derive()` debug helper(可后续删)。

**对照 Flap 5 步发币现状（user 2026-06-28 核对）:** ①logo→pin IPFS ✅;②标准 metadata JSON ✅;③Metaplex 写链上 uri ✅(机制);④revoke mint+freeze+immutable metadata 🟡(**无 freeze authority 已做**=比 revoke 更干净;revoke mint 曲线期不能做、得等毕业;immutable metadata 没做);⑤建池+Jupiter Verify 🟡(自带 bonding curve ✅;Raydium 迁移是 stub;Jupiter Verify 没做)。

**✅ Flap 步骤① 收尾完成（2026-06-28）—— IPFS uri 真正写上链:** 之前顺序错(先发币 uri=服务器端点→后 pin),现已改成 **先 pin IPFS 拿 `ipfs://CID` → 再写进 create_token**。改动:`launchpad.js` 抽出纯函数 `metadataJsonFrom({mint,name,symbol,image,website,twitter,telegram,description,creator})`(不依赖已存在 launch 记录,发币前可用),`tokenMetadataJson` 改为调它;`create()` 加 `metadataUri` 入参。新路由 `POST /api/coin/pin`{mint,name,symbol,logo,...}→pin logo(pinImageFromUrl)+JSON(pinJson)→返回 `{uri:ipfs://,gateway,image}`(无 PINATA→`{devMode}`)。`chain.js`:`createToken` 加 `{uri,mint}` 入参(可传预生成的 mint Keypair)+ 导出 `newMint()`。`launch.html` createBtn 新流程:`OneChain.newMint()`→`POST /api/coin/pin`(按钮显「Pinning to IPFS…」)→`metadataUri=pin.uri||服务器端点 fallback`→`OneChain.createToken({uri:metadataUri,mint})`(显「Minting on Solana…」)→`POST /api/launch{mint,mintTx,metadataUri,logo:gateway}`;成功页显示 IPFS pin 状态。**post() 不抛非2xx→pin 失败/devMode 自动 fallback 到 metadata 端点,不中断发币。** 验证:`metadataJsonFrom` JSON shape 正确、refactor 没破 `/api/coin/metadata`(regression ✅)、`/api/launch` 存住 `metadataUri:ipfs://`+`onchain:true`、preview 里 `OneChain.newMint()` 出 44-char keypair、`derive()` 仍逐字节匹配、0 console error。**⚠️ 真 IPFS pin 这次没跑通——本 sandbox server 进程 outbound 到 api.pinata.cloud "fetch failed"(网络限制,非代码 bug;PINATA_JWT 在 .env 且 user 环境实测可用)。** 现在 Flap 步①②③ 完全闭环(logo 永不裂图+全钱包显示+永久去中心化 uri)。**✅ Flap 步④ 代码完成并编译通过（2026-06-28，未部署）:** 改的是**真部署源** `C:\Users\user\kol-meme\onchain\programs\kolpad\src\lib.rs`（注意:`C:\Users\user\kolpad\` 是旧副本,declare_id 还是占位 `Ko1pad…`、无 graduate——**别改那个**;真源在 kol-meme/onchain,declare_id=5SVSaKce、有 graduate/launch fee/creator-fee split）。在 `graduate()` 里加两个 CPI（都用 launch PDA seeds 签名,因为它是 mint+metadata 的 authority;曲线买入毕业后已停=供应量已固定,所以此时 revoke mint 安全）:(1) `token::set_authority(mint, MintTokens, None)` → 永久 revoke mint authority（再不能增发）;(2) `update_metadata_accounts_v2(is_mutable=Some(false))` → metadata 永久 immutable（名/符号/logo uri 改不了）。`Graduate` accounts 加 `mint`(`address=launch.mint`)、`metadata`(UncheckedAccount mut)、`token_program`、`token_metadata_program`;imports 加 `update_metadata_accounts_v2, UpdateMetadataAccountsV2`。**编译验证:** Docker `backpackapp/build:v0.30.1` + 挂 `kolpad-cargo:/root/.cargo`(=完整 /root/.cargo,含 cargo+anchor) + `onchain:/workdir`,`anchor build --no-idl` → **Finished, 0 error, 1m48s**,`.so` 重建 412592 bytes。**关键 build/deploy 信息:** kolpad-cargo 卷=/root/.cargo;kolpad-solana 卷里 deployer keypair 在 `/v/solana/id.json`（挂 `kolpad-solana:/root/.config`→默认 signer `/root/.config/solana/id.json`）;host `onchain/.deployer.json` 也有。**升级命令(一条,未跑——会动 live 程序+花~2.8 devnet SOL,且 graduate 要 85◎ 真储备没法 e2e 测):** `docker run --rm -v C:\Users\user\kol-meme\onchain:/workdir -v kolpad-cargo:/root/.cargo -v kolpad-solana:/root/.config -w /workdir backpackapp/build:v0.30.1 sh -c "solana config set --url https://api.devnet.solana.com && solana program deploy target/deploy/kolpad.so --program-id target/deploy/kolpad-keypair.json"`。**Flap 5 步现状:①②③ 闭环 ✅;④ 代码+编译 ✅(待部署);⑤ 仍缺真 Raydium 迁移+Jupiter Verify。** **下一步:** 部署步④升级(user 定)、真钱包实测发币、Flap 步⑤、买/卖/赎回搬浏览器、Studio 真登录、NFT、真实 Stripe、mainnet。

**✅ oneIP.ai 前端正门已建（2026-06-28, preview 验证全过）：** `web/ai.html`（复用品牌套件:cyan→violet→magenta→coral 渐变、Onee 吉祥物、Plus Jakarta+Noto SC+Space Grotesk;EN/中文 i18n;**body 零币字样**已验证）。结构:Hero「Creators sell the world's best products / 网红带你买全世界好物」+ How-it-works 卡(选品→刷卡→发货→20%壮大价值池)、商品网格(fetch `/api/shop/products` 渲染卡片:创作者头像/标题/价格/Buy now)、创作者 rail(按 creator 分组,带「Value pool →」深链 `coinUrl()`→`.io`/coin)、透明度 band(「Every sale builds real on-chain value」"not a payout" utility 措辞)。买流程=法币 modal(邮箱/姓名/国家[东南亚预设]/地址)→`POST /api/shop/order`→Stripe url 跳转 或 devMode→`/api/shop/dev-paid`→支付成功页(含「订单结算后20%注入价值池·链上可查」util 提示)。`IO_BASE`=`location.hostname` 判断:`.ai` 域→`https://www.oneip.io`,dev 同源→相对路径。server.js 加路由 `/ai`、`/ai.html`、`/shop`、`/shop.html`→ai.html。preview 实测:4 个 seed 商品 + 2 创作者渲染正常,浏览器内走通 买→下单→supplier mock→paid,0 console error。Seed 了 4 个 demo 商品在 `data/products.json`(AuroraTV/NovaPlays,可删)。**预览启动**:`C:\Users\user\.claude\.claude\launch.json` 有 `oneip` 配置(`npm --prefix C:/Users/user/kol-meme run serve`, port 8787)。**TODO 深化 .ai：** 粉丝互动/发帖目前仍在 `/feed`(feed.html Creator Studio),.ai 暂用 Community 链接过去,后续可把 feed 并进 .ai 外壳;真实 Stripe + 东南亚本地支付;商品图片上传;.ai 自己的网红入驻/选品后台(现用 `POST /api/shop/product` wallet-trusted)。

**✅ 自动发币 + 一账号两边通（user "必须补充" 2026-06-28, HTTP+UI 验证全过）:** 需求=在 oneIP.ai **开户后,只要生成/发布任何产品、或一键转发 14 社交平台,就自动在 oneIP.io 给同一个账号生成 value-pool 代币;同一账号两边通。** 实现核心抽象 `ensureValuePool(account)`(server.js,**账号字符串无关**——handle 或 wallet 都行,正好满足"只要用同一个账号就两边通"):无 coin 则 `launchpad.create({creator:account, symbol:deriveSymbol(account), auto:true})`(占位 mint)+ `social.setCreator(account,{coin})` 链接,幂等返回 coin。`deriveSymbol`=账号去非字母数字、大写、≤8、不足补 "IP"。自动触发点:`POST /api/shop/product`(列产品,且把 `b.coin` 空时 stamp 上自动池)、`POST /api/posts`(发帖)、`POST /api/publish/now`(转发14平台)。`launchpad.create` 加 `auto` 标记。**预发行 escrow**:`buyback.execute` 对占位 coin(`startsWith("Kol")`,无链上 vault)记 `status:"escrow"`(累计、不假装灌地板),等真发币 flush(TODO flush job);`proof` 加 `escrowCount/escrowUsd`(与 funded/queued 分开)。studio.html `loadDashboard` 改从 server `creator.coin` 拉(自动池显示在工作台,pCoin 预填)、`renderVpool` 对占位池显示 escrowUsd + "已从你的销售托管·链上发币即激活" pending 态。**验证(HTTP):** 新账号列1个无-coin产品→product.coin 自动 stamp `Kol…`、creator.coin 链接、`/api/launch` 有记录(symbol=NEWBIECR,auto=true,onchain=false,creator=账号)、卖一单 settle→buyback.status=escrow、proof escrowUsd=$2.82。**(UI):** Studio 登录→列产品→刷新→value pool 自动出现(pCoin 预填占位 coin + pending 文案 + 「在 oneIP.io 查看 →」深链),0 console error。commerce:smoke 无回归(SmokeCoin 不以 Kol 开头→仍 funded/queued)。**身份模型(已落地 2026-06-28):** **邮箱注册 = 主账号(handle),钱包待会再绑(上链时才需要)** —— user 拍板 "用邮箱注册-再绑钱包"。新 `src/accounts.js`(零依赖,data/accounts.json,`{accounts:{handle:{handle,email,salt,hash,wallet,createdAt}}, sessions:{token:handle}}`):`register({handle[2-20 alnum_],email,password[≥6]})`→scrypt 哈希+salt+session token、`login({id=email或handle,password})`、`me(token)`/`resolveToken`、`bindWallet(token,wallet)`、`logout`;publicAccount 不漏 hash/salt。server.js 路由 `/api/account/{register,login,me,logout,bind-wallet}`;register 顺带 `social.setCreator(handle,{email})`。**handle 仍是全站 account 字符串**(shop/studio/brands/ensureValuePool 全部不变,邮箱+密码只是加在上面的真注册层 + 钱包绑定)。studio.html 登录改为 **注册/登录 seg**(注册=handle+email+password;登录=email或handle+password),存 `oneip_token`+`oneip_creator`;value pool 区加「绑定 Phantom 钱包」按钮(connect→`/api/account/bind-wallet`,加载 wallet.js)。**验证(HTTP):** 注册(hash 不漏)、重名/坏邮箱/短密码全拦、邮箱登录 ok/错密码拦、绑钱包后 /me 显示、注册的 handle 当 account 列产品→自动池+account.email 在 profile。**(UI):** studio 表单注册→dashboard(who=handle,token+creator 存好),0 console error。**注意:** 服务端 mutation 暂未强制校验 token(仍 wallet/handle-trusted MVP);邮箱目前无验证邮件/找回(后续硬化)。feed 的"连钱包才能发帖"还没换成邮箱账号(下一步可做)。

**(旧注)身份模型 account-agnostic:** `ensureValuePool` 对 account 字符串无关,所以 handle/wallet/email 都能用 —— 现在统一用邮箱注册出的 handle。**✅ escrow flush + 真发币迁移完成（2026-06-28, HTTP 验证全过）—— "同一个账号两边通" 贯穿到真实链上代币:** 账号在 .ai 用占位池攒了 escrow 后,**用同一账号在 /launch 真发币时**,一键迁移:`buyback.flushEscrow(fromCoin,toCoin)`(escrow 记录 re-key 到真 mint + 标记 funded;用 Set 按引用删旧避免重复 bug)、`source.repointCreatorCoin(creator,from,to)`(账号所有产品 coin 切到真 mint)、`social.setCreator(account,{coin:真mint})`、旧占位 launchpad 记录标 `superseded`。server `activateRealToken(account,realMint)` 串起来,`POST /api/launch` 收到 `{account, mint}` 时调用(并把 `migrated` 回传)。前端:`launch.html` 读 `?account=` 传进 /api/launch;`studio.html` 的「去 oneIP.io 发币」深链带 `?account=<handle>`。**验证(HTTP):** 账号列产品(占位池)→卖一单 settle→escrow $4.17;同账号 launch 真 mint→`migrated:{flushed:1,lamports:27.8M,repointed:1}`;之后 真coin funded=1/floor=0.0278◎、占位池 escrow=0、creator.coin+product.coin 都=真 mint。commerce:smoke 无回归。**仍 TODO:** 真模式 flush 应真在链上灌 floor vault(现 mock re-key)、.ai Studio 自带 14平台转发 UI(现转发在 /feed wallet-based)、handle↔wallet 显式 link(现 launch 的 creator=wallet、account=handle 各传各的,够用但没建立持久绑定)。

**✅ oneIP.ai 供给侧（厂商/供应链/品牌）已建（2026-06-28, HTTP+DOM 验证全过）:** 需求=给 .ai 加供给侧,让厂商/供应链/品牌入驻、发布需求/代言/产品推广、设价格+联系号码,创业者(网红)链接品牌(产品类落进店铺)。新 `src/commerce/brands.js`(零依赖,data/{brands,offers,brand-links}.json):`registerBrand({name,type∈factory/supply/brand,contact[必填],logo,bio,website})`、`addOffer({brandId,kind∈product/endorsement/need,title,priceCents(product必填),description,image,contact,moq})`、`listOffers({kind})`(join 品牌 name/type/logo/contact)、`setOfferActive(id,brandId,active)`(owner-guard)、`linkBrand({creator,offerId})`(去重)、`listLinks`。server.js 路由:`GET/POST /api/brands`、`GET /api/brand?id=`(含其 offers)、`GET /api/offers?kind=`、`POST /api/brand/offer`、`POST /api/brand/offer/active`、`GET /api/brand/links?creator=`、**`POST /api/brand/link`**(核心编排:creator 链 product offer→`ensureValuePool(creator)`自动建池 + `shopSource.addProduct`自动落店铺,cost=供货价、retail=传入价或默认 2x、supplier=brandId)。新页 `web/brands.html`(路由 `/brands`、`/brands.html`,品牌套件,EN/中文):mode 切换「浏览机会(创业者)/我是品牌(供应商)」;浏览=kind tabs(全部/产品/代言/需求)+ offer 卡(品牌头像/类型、kind 徽章、标题、供货价、📞contact、「链接品牌」按钮,link 用 localStorage `oneip_creator` handle,没有则 prompt);品牌门户=入驻表单(名称/类型/联系/官网/简介,localStorage `oneip_brand`)→发布 offer(kind seg、标题、价[product]、描述、图、contact)→我的 offers(在售/隐藏 toggle)。ai.html 导航加「Brands/品牌」→`/brands`。**验证(HTTP):** 注册 factory 品牌(带 contact)→发 product offer($12供货价)+endorsement offer→`/api/offers` 两条都带品牌+contact→creator link product→自动店铺商品(retail $24/cost $12/supplier=brandId/coin 自动占位池)+creator.coin 自动设→商品进 GamerNova 店铺、endorsement 过滤正常。**(DOM):** /brands 渲染 2 offer(徽章「代言」、「📞 +86…」、「链接品牌」)、品牌门户 toggle 出注册表单,0 console error。**闭环串联:** 品牌发 offer→网红链接→产品落店铺→自动发币→粉丝买→20% escrow→真发币 flush。**TODO:** 代言/需求类 link 后的洽谈流程(现仅记 link+给联系方式)、品牌侧也接 ensureValuePool? (暂不)、Studio 里也露 Brands 入口。

**✅ 共享页头组件 + 终端 i18n 修复（2026-06-28, user 投诉"每页按钮大小不规律/语言乱"）:** 病因=两代设计混用——home 是新版(渐变/EN中文胶囊/有Shop),terminal/launch/feed/coin 是旧版(衬线logo/下拉或幽灵按钮/没Shop/Connect Wallet 样式各异),且**两套 i18n**(KolI18n 7语言 vs home/.ai 内联2语言,都用 `data-i18n` 属性会冲突)。**修法:做了共享页头 `web/header.js`**(单一真源,注入自带 CSS=home 同款渐变,自渲染 logo+nav[Home/Terminal/Launch/Creators/Shop,Shop 域名感知跳 .ai]+7语言下拉[KolI18n]+渐变 Connect Wallet[KolWallet];按 pathname 高亮 active;`window.onLangChange` 桥接;`#appHeaderExtra` 插槽给页面特定控件)。server.js 静态路由加 `/header.js`;i18n.js 加 `nav_shop`(en"Shop"/zh"逛世界")。**已套用 4 个旧页**:index/launch/feed/coin —— 各自 `<header>` 换成 `<div id="appHeader">`、加 i18n.js+wallet.js+header.js、删掉旧 connectBtn/langSel 内联 wiring(保留页面专属逻辑:launch 的 f-creator 自动填、feed 的通知 bell 移进 appHeaderExtra 插槽、coin 的 composer)。**还修了终端写死中文**:tabs(热门/新币/涨幅)、底部、空状态加 i18n key(mkt_hot/new/gainers/loading、foot_data、coin_no_pair、board_no_coins,en+zh,其它5语言回退英文不再串中文)。**验证(DOM,全过 0 err):** terminal/launch/feed/coin 都渲染统一页头(5 nav、7语言、active 正确、渐变 Connect);feed 的 bell 进了插槽、通知/composer 仍工作;launch 的 createBtn/f-creator 仍在;终端 tabs en=Hot/New/Gainers zh=热门/新币/涨幅,泰文回退英文。截图确认终端页头 = home 同款。**✅ 自定义语言下拉 + home 并入统一页头（2026-06-28, user 投诉"语言按钮难看"+继续统一）:** (1) header.js 的原生 `<select>` 换成**自定义下拉**(`.oneip-lang`:暗底圆角触发钮 + 深色面板菜单 + 当前语言渐变高亮+✓ + 点外关 + 选完更新 label/nav);一处改 4 个旧页全生效。(2) **home.html 也并入共享页头了**(低风险方案):把 home 自己的 i18n 属性 `data-i18n`→`data-hi18n`(replace_all)避开和 KolI18n `data-i18n` 选择器冲突,保留 home 内联 en/zh 文案系统;`<header>` 换 `<div id="appHeader">`、加 i18n.js+header.js、删旧 connectBtn/nav-scroll/EN中文胶囊 wiring;home `setLang` **不再写 localStorage**(让 KolI18n 独占 `kol_lang`,避免覆盖 7语言选择);`window.onLangChange=setLang` 桥接(header 下拉→home body)。**效果:5 个 .io 页(home/terminal/launch/feed/coin)现在共享同一个页头 + 同一个 7语言下拉。** home body 仍 en/zh(选 th 等→英文回退,可后续全译)。**验证(DOM+截图 0err):** home 渲染统一页头(5nav/7语言下拉/渐变连钱包)、hero data-hi18n-html 渐变 span 保留、「粉丝情绪价值已上链」卡片在、旧胶囊没了;header 下拉切 en/zh→header nav + home body(hero/card/nav)同步切换。**✅ 终端侧栏中文也 i18n 干净了（2026-06-28）:** i18n.js 加 mkt_title/oneip_board/floor_tag_20/smart_kol/loading_w/no_data/floor_word/socials_word(en+zh);index.html 把面板标题(全网SOL Meme/oneIP榜/带20%底池/聪明钱·KOL/加载中)包进 `data-i18n` span,JS 渲染的(暂无数据/% 底池/N 社交)改用 `KolI18n.t()`,并加 `window.addEventListener('i18n',()=>{loadMarkets();loadOneip();loadKol();})` 让动态列表语言切换时重渲染。验证:英文模式终端 body **零 CJK**、zh 仍对、0 err、截图确认(All SOL memes/No data/oneIP board/with 20% floor/20% floor/0 socials)。仅 `<title>` 标签保留品牌中文(浏览器 tab,无妨)。

**✅ .ai 也做了共享页头（2026-06-28）:** 新 `web/aiheader.js`(自带 CSS=oneIP.ai 渐变品牌,logo 🛍️ oneIP.ai + 导航 Shop/Brands/Studio/Community[按 pathname active] + EN/中文 胶囊 toggle + 渐变「Start selling」CTA;自带 en/zh 小字典翻自己的 label;`window.onLangChange` 桥接页面 body i18n)。server.js 静态路由加 `/aiheader.js`。**套用 ai.html/studio.html/brands.html**:各自 `<header>`→`<div id="aiHeader">`、加 `<script src="aiheader.js">`、定义 `window.onLangChange=setLang`、其 setLang **不再写 localStorage**(aiheader 独占 kol_lang)。验证(DOM+截图 0err):三页都渲染同一 .ai 页头(active 各自 Shop/Studio/Brands、4 nav)、切 EN/中文 时 header nav + body(hero 等)同步、brands offers 仍载、studio dashboard 正常。**现在 .io(header.js)和 .ai(aiheader.js)各有一个共享页头,各自内部全一致。** 注:窄屏(<760px)nav 收起只剩 logo+lang+CTA。

**✅ feed 帖子加币 CTA 转化卡（2026-06-28）:** 帖子本来就能挂币(`p.coin`,social.js feed() 返回 coinCard{mint,symbol,name,verified,floorBps}),但只渲染了个小 `$SYMBOL` 角标。现在在帖子图片下、like 行上加一张**醒目转化卡** `.coincta`:渐变卡显示 `$SYMBOL` + 名称(+✓ if verified) + `20% floor` 徽章 + 渐变「Trade →」按钮,整卡 `href=/coin?mint=`。让 feed 从纯展示变**转化点**(刷到帖→点 Trade→币页)。验证(DOM+截图 0err):给 AuroraTV 发带币帖→渲染出 `$AURORATV·20% floor·Trade →` 卡、深链 /coin?mint=Kol…。**注:商品 CTA 没做**——帖子挂商品需要 post.product 字段 + 取创作者商品,但 feed 帖创作者=钱包、.ai 商品=handle,身份没统一会取空,所以先只做币 CTA(币是 .io 原生、数据现成);商品挂帖待 handle↔wallet 统一后再做。

**✅ handle↔wallet 身份统一 + 帖子挂商品 CTA（2026-06-28, HTTP+UI 验证全过）:** (1) **身份链:** accounts.js 加 `handleForWallet(wallet)`/`byWallet(wallet)`(按绑定的钱包反查 handle);server `GET /api/account/by-wallet?wallet=`→{handle,...}。(2) **帖子挂商品:** social.js `import commerce/source`,`createPost` 加 `product`(productId)字段,`feed()` 加 `product: productCard(p.product)`(productCard=source.getProduct→{id,title,image,priceCents,currency,type,creator})。(3) **feed.html:** 帖子渲染**两种转化卡**——`.coincta`(币:$符号+20%floor+「Trade →」→/coin?mint=,之前做的)+ 新 `.prodcta`(商品:缩略图+标题+$价+「Buy →」→ `AI_SHOP`= .io 域跳 https://www.oneip.ai 否则 /ai);composer 加 `<select id=postProduct>`,`loadMyProducts()` **用 `/api/account/by-wallet` 把连的钱包解析成 handle → 取该 handle 的 `/api/shop/products` 填下拉**(这就是身份统一的用处:钱包侧 feed 能找到 .ai 账号的商品);submitPost 带 product、发完清空两个 select。**验证:** HTTP——注册账号+绑钱包→`by-wallet` 解析回 handle ✓、handle 下列商品→以钱包发带 product 帖→feed payload 含商品卡(Unify Hoodie/$45)✓;UI——feed 同时渲染商品卡(Buy→/ai)+币卡(Trade→),0 console error,截图确认两张卡都漂亮。**身份模型现在完整:邮箱注册=主账号(handle),绑 Phantom 钱包,两者互查;钱包侧(.io feed)能解析到 handle 从而拿到 .ai 商品。**

**TODO（残留）:** home body 只 en/zh(要全译搬进 KolI18n DICT);`.ai` body i18n 也 en/zh(符合定位);商品「Buy →」目前跳 .ai 商城首页(没有单品详情页,可后续加 `/ai?product=` 高亮或单品页);上线 prep(等用户绑卡)。**✅ 上线 code prep 完成（2026-06-28）:** server.js 根路由 `/` 按 `req.headers.host` 分流——含 `oneip.ai` 且 path==="/" → 服务 `ai.html`(商城),否则 → `home.html`(.io 首页);`/home`、`/home.html` 永远 .io 首页。curl 验证:Host www.oneip.ai→ai.html、Host oneip.io→home.html。一套服务两个域名都指它即可。部署=Render(build `npm install --omit=dev --omit=optional`,start `npm run serve`,server 读 `process.env.PORT`),两域名都加到同一 service,DNS 按 Render 给的记录填。仍待用户实操:GitHub push / Render 连仓库 / 填 env / 注册商加 DNS。

**✅ 打赏换成付费 emoji 表情 + 5% 平台费（2026-06-28, HTTP+DOM 验证全过）—— user idea「web4 likes」:** 概念=web2 点赞免费没价值,oneIP.ai 里每个"赞"都付费=粉丝情绪变真实收入;打赏太硬,换成 5 个带价 emoji 更好玩。**5 个表情(价格阶梯):** 👍赞$0.5 / ✌️V手$1 / 💪加油$2 / ❤️爱了$5 / 🔥燃爆$10。每个表情=一笔小额付费,走和 tip 一样引擎(**5% 平台费 PLATFORM_TIP_FEE_BPS + 支付费,剩下 20%灌地板 + 创作者**)。**先做了 tip 的 5% 平台费**(`orders.createTip` 加 platformFeeCents=5%,smoke 验证 $20→扣$1)。新 `src/commerce/reactions.js`(REACTIONS 配置 + per-target 计数 data/reactions.json + bump/getCounts);`orders.createReaction`(type"reaction",instant settle,带 emoji/target/reactionKey,economics 同 tip 含 5%)。server.js 路由:`GET /api/reactions/config`、`GET /api/reactions?target=`、`POST /api/react`{target,creator,coin,key,fan}(有 Stripe→checkout pending[count 待 webhook bump,TODO];无→instant settle+bump;coin 空→`ensureValuePool` 自动建池)。target=产品id/帖id/创作者handle 都行,计数 per-target。**ai.html UI:** 替掉「💝 Tip」按钮——`reactionBar(target,creator,coin)` 渲染 5 个 emoji pill(emoji+count,title 显价),`react()` 点击→POST /api/react(dev instant,live Stripe 则跳 checkout)→更新计数+pop 动画;`refreshReactions()` 按 target 拉计数填充;商品卡 + 创作者 rail + drawer 都挂了 reaction bar。init 先 `loadReactions()` 拿配置再 loadShop。**验证:** HTTP——config 5 个、react 3×👍+1×🔥→counts{like:3,fire:1}、🔥$10→platformFee $0.5(5%)+settled+20%escrow ✓;DOM——/ai 14 商品+10 创作者都有 emoji bar、点👍两次 count 0→2、0 console error ✓。**✅ reaction UI 改成「单触发器 + 弹出选择器」（2026-06-28, user 嫌 5 个 emoji 铺卡片上太满）:** 卡片/创作者上不再铺 5 个 emoji pill,改成**一个干净的小触发器**(心形 SVG + 总数,像 feed 帖底部 ♡ 0 的样式,`.rxtrig`,有则填红 `.reacted`);**点击弹出一个小 popup 选择器**(`.rxpicker`,单个共享 fixed 元素,定位在触发器上方/下方,5 个 emoji 竖排 emoji+价,`.rxpick`,入场动画 rxin),选一个 → `react()` 付费 → popup 关 → 触发器总数 +1 + pop 动画;点外面/滚动/resize 自动关。`reactionBar`→`reactionTrigger`(全 3 处调用点已改),`paintCounts`→`paintTotals`(总和),新增 `openRxPicker/closeRxPicker/pickReaction/ensurePicker`。验证(DOM 0err):/ai 每卡 1 个触发器(.rxbar 归 0)、点开 picker 出 5 emoji+价($0.5/$1/$2/$5/$10)、选💪→react→总数 4→5、选完 picker 关。**注:preview_screenshot 在 /ai 必超时(blur 图层),只能 DOM 验证。**

**✅ 后台管理升级成平台运营 dashboard（2026-06-28, HTTP+DOM 验证全过）:** 旧 admin.html 只有 KOL 钱包 + 发币认证,完全没覆盖后建的商城/订单/收入/品牌/表情。新增后端 `GET /api/admin/overview`(admin-gated)聚合:accounts(accounts.list 新增)、products、orders 总数+settled+byType{physical/digital/tip/reaction}、GMV(settled priceCents 和)、**platformRevenueUsd(settled platformFeeCents 和=5%打赏/表情费)**、floorFunded/escrow/queued(从 order.buyback.status 分桶)、reactions(reactions.totalReactions 新增)、brands/offers、launches/verified、treasury.snapshot。admin.html 加:9 张 overview 卡、**Orders 表**(类型/创作者/标题/金额/状态 + 按状态给 Ship/Deliver/Settle/Refund 按钮 → 调已有 /api/admin/shop/{ship,deliver,settle,refund})、**Treasury 区**(float 余额 + funded/escrow/queued + top-up 输入→/api/admin/shop/treasury/topup)、**Brands 表**。沿用旧的 wallets+launches 段。token 仍 `Qwer1234@admin`(.env),localStorage `oneip_admin` 自动登录。验证:overview HTTP(accounts6/products15/orders26/GMV$233/platformRev$2.9/floorFunded$2.6/reactions15/treasury2.98SOL/byType对)+无 token→401;UI——dash 渲染 9 卡+26 订单行+treasury 余额/流水+brands,0 console err。**注:admin.html 还有几个 Tabler 图标(discover/check)在本环境不渲染(内部页,低优先);screenshot 本会话普遍超时,靠 DOM 验证。**

**🔻 砍掉 Pro 订阅 —— 创作者工具全免费（user 决定，2026-06-29，作废前述 Pro-gating 描述）:** $10(AI clone)/$39(14平台发布) 两档 Pro 订阅**已取消**,创作者工具(AI 草稿/自动回复、多平台发布、Studio)现在**全部免费**。**⚠️ 本文件前面多处描述的 Pro/proTier/hasAi/hasPublish/402-if-not-pro/"Pro tier price raised $10→$39"(见第14、32、36行附近)均已作废**——别再当真相,以本条为准。平台收入模型现在只剩:(a) 商品/digital/NFT 销售净利的费;(b) 打赏 + 付费表情各抽 **5% 平台费**(`PLATFORM_TIP_FEE_BPS`,见第74、76行,这条仍有效);(c) 链上 0.1◎ launch fee + 1.25% trade fee(0.75 平台/0.5 创作者)。**复查代码是否还有 Pro-gating 残留(可能的 TODO):** server.js publish/AI 路由的 `hasPublish`/`hasAi` gate、feed.html 的「Upgrade to Pro」按钮/PRO 徽章、social.js 的 `proTier`/`setTier`、billing.js 的 `STRIPE_PRICE_ID_PUBLISH`——若仍在需清掉或确认已拆(新 session 应先 grep 核实再下结论)。

**TODO:** Stripe live 时 react 的 count 要在 webhook 里 bump(现 dev instant);旧 tip modal 代码留着没删(不再被调用);.io feed 帖也可挂 reaction(现仅 .ai);admin 可继续加(账号/产品 CRUD、退款明细、表情明细、导出)。

**💰 收益分配定案 v1（user 2026-06-29 逐项确认，作废前面所有旧分成数字）—— 平台收益 + 代理 + 1代推荐 + 创业者四方模型，两个域名各一套底:** 核心原则=**所有渠道分成都从"平台那一池"里切，绝不在其上叠加、绝不从创业者或地板里挖**(同链上 creator-fee 从 platform-fee 切的哲学)；商家/买家永远只付那个固定费率，"全行业最低费"叙事不破。**(A) oneIP.ai(电商，底=成交净利=售价−供货成本−支付费):净利 100% 拆 → 创业者 75% / 持币人地板 20% / 平台服务费 5%(三者和=100%，一个底切三刀)。** 那 5% 平台池再拆 4 方(均为净利的绝对%，和=5%):平台基础 2.5% / 国家代理 0.5% / 地方代理 1.0% / 推荐1代 1.0%。空缺方(无代理/无推荐人)→平台吃。**⚠️ 注意现行代码 `orders.js` 还是旧的"5%收毛额 + 剩下按20/80切"，要改成这个"净利→75/20/5"干净模型(创业者 80%→75%、5%改收净利而非售价)。** **(B) oneIP.io(代币交易，底=每笔买/卖 1.25% 交易费):创业者 0.5%(链上直发) + 平台池 0.75%。** 平台池 0.75% 再拆 4 方:平台基础 0.40% / 国家代理 0.05% / 地方代理 0.15% / 推荐1代 0.15%。**地板另从交易本体来:买入 20% / 卖出 2% → floor vault(归持币人，不进任何人口袋，铁律不动)；发币费 0.1◎→平台池。** .io 上创业者真正回报=持有自己公平买入的币(有电商现金流灌地板撑)，0.5% 只是交易费分成，所以 .io 不套 75/20/5。**.io 的 4 方拆分走链下结算(别动已部署的链上程序，链上仍把 0.75% 打给一个 fee_recipient，平台用 Helius 索引每笔交易→币→创业者的代理/推荐人，链下从这池子定期结算)；和 .ai 共用同一套 payouts 台账。** **卖出 2% 的妙处(user 赞"妙计"):有人砸盘卖币→抽2%灌地板+其币退出供应→留下的持币人每份可赎回额反而升高="砸盘反抬保底"，whale 砸不穿(=记忆里"永不归零"的来源)；买20%是钱流入时大力建垫，卖2%是温和退出摩擦(若卖也抽20%=honeypot/高退出税会吓跑人)。**护栏(守反庞氏/反证券铁律):代理硬封顶 2 层(国家→地方，地方代理不可再生子级，靠任命+专属辖区+真实GMV，非拉人头)；推荐硬封顶 1 代(不延伸至被推荐人之后推荐的人)；所有奖金法币发放、不挂币价、不用代币结算；只奖励真实成交、绝不奖励拉人头/拉充值。** **推荐奖金期限(user 拍板):不限期，但被推荐创业者 60 天内无真实结算成交(有GMV，非登录)→ 推荐绑定永久关闭(选项A，即使其日后回归也不再分成)；防僵尸链、绑"持续真实经营"。** **合法措辞(对外一律用，绝不出现分红/下级/层级/躺赚/投资):** 平台服务费 Platform service fee；代理→授权区域伙伴 Authorized regional partner，其收入→渠道服务佣金 channel service commission(框成"为辖区招商/本地支持服务付的对价")；推荐→老带新推荐奖励 refer-a-friend reward；创业者收入 Creator earnings。条款须含免责:服务佣金与推荐奖励均为基于真实交易的商业佣金，非证券/非投资/无收益承诺，单层直推、平台不运营任何多层级报酬计划。代码内部命名也用干净词(partnerCommissionCents/countryPartnerCut/referrerHandle/referralRewardCents/territory，别用 downline/upline/level/tier)。**✅ 后端已实现并验证(2026-06-29, smoke + HTTP 全绿):** `orders.js` 改成统一 75/20/5 净利模型(`computeNet` 对 product/digital/tip/reaction 一视同仁:净利=售价−供货−支付费=100%,拆 creator 75%/floor 20%/platform 5%,和精确等于净利;每单存 `creatorCents/floorCents/platformFeeCents/netProfitCents`);buyback 仍取净利 20%(BUYBACK_BPS 2000 不变,语义一致)。5% 池的 4 方子拆延到 **settle** 才算(那时才知道代理/推荐+60天闸门结果):`splitPlatformPool` 按 net 的 bps 切 country 0.5%/local 1.0%/referral 1.0%,platformBase=池子减这三块(空位全回落平台),记在 `order.payout` 上;常量 `POOL_COUNTRY_BPS/POOL_LOCAL_BPS/POOL_REFERRAL_BPS`(可 env 调)。新 `src/commerce/agents.js`(2层封顶:country 无 parent、local 必须挂已存在的 country 当 parent,local 不能当 parent→第3层结构上不可能;`register/list/setActive/resolveAssignment`)。`accounts.js` 加 `referrerHandle`(register 时一次性写死,必须是已存在且非自己的 handle,坏/自荐静默丢弃)+ `agentCountry/agentLocalId` + `lastSettledSaleAt/referralClosed`;新 `assignAgents(handle,{countryId,localId})` 和 `resolveSaleParties(creator,saleTs)`(creator 可 handle 或钱包;**60天闸门=两次结算成交间隔≥60天则永久关闭推荐绑定[选项A],此单及之后不再给推荐人**;每次 settle 调一次、幂等)。`orders.payouts()` = 按代理id/推荐handle 聚合欠款台账。server.js 接了 admin 路由:`GET/POST /api/admin/agents`、`POST /api/admin/agents/active`、`POST /api/admin/agents/assign`(指派 creator→local,country 从 parent 派生)、`GET /api/admin/payouts`;`/api/admin/overview` 加 `platformPoolUsd`(整 5%)、`platformRevenueUsd`(=池−代理−推荐,平台真实留存)、`partnerPayoutUsd/referralPayoutUsd/agents`。register 路由已透传 `referrer`(前端传即生效)。**验证:** `npm run commerce:smoke` 扩到覆盖 75/20/5 精确加总+4方子拆+空位回落+2层封顶+60天闸门(gap 30天仍活/61天永久关)+payouts 台账,全绿;HTTP 实测 register country/local、2层封顶被拒、referrer 捕获、assign 派生 country、一单 $96.80 净利→国家$0.48/地方$0.97/推荐$0.97/平台$2.42(池$4.84=5% 精确)、overview 对账 pool=keep+partners+referral 精确闭合。**分账后端 DONE。** **残留(前端/后续):** admin.html 加代理管理+payouts 展示卡(现有后端路由无 UI);前端注册页 `?ref=` 捕获推荐人并带进 register;studio/brands 露代理入口;**.io 链上 0.75% 的 4 方拆分走链下 Helius 索引结算(未做,放后面,别动已部署程序)**;真 Stripe 上线后 platform/agent/referral 结算改 webhook。**⚠️ 本次 HTTP 测试往 data/{agents,accounts,products,orders,buybacks}.json 写了测试数据(PH Country/crt*/inv* 等),可清。**

**✅ 铁粉一键赎回地板 UI 已建（2026-06-29, preview 对活链验证）—— "价格到地板,持币人烧币按比例领回金库 SOL":** 需求=之前只有链上 `redeem_floor` 指令(已部署)没有网页入口,补上。`web/chain.js` 加两个方法(照搬已证 account 顺序,浏览器 web3.js):`redeemQuote(mint,owner)`=纯标准 RPC 预估(不解析链上结构):读金库余额 `getBalance(vault)` + 总量 `getTokenSupply` + 持仓 `getTokenAccountBalance(ATA)` → est = vaultSol × (持仓/流通);`redeemFloor({mint,rawAmount?})`=构造 RedeemFloor 指令(账户序 launch/mint/vault/holderToken(ATA)/holder/token_program,disc=redeem_floor,u64 数量默认全仓),Phantom 签 holder+feePayer,confirm 返回 tx。新增 `borshU64`、`ataFor(owner,mint)`(标准 ATA 派生,无需 spl-token import,ASSOCIATED_TOKEN_PROGRAM_ID=ATokenGPv…)。`web/coin.html` 加 `<script type=module chain.js>` + header 卡内**赎回面板**(仅真链上币显示:`isOnchainCoin`=`c.onchain||mint 非 Kol 开头`):「查看我的地板份额」→ `checkRedeem()` 连钱包+quote→显示 持有/占比/预估可赎回◎ + 「赎回全部」按钮→ `doRedeem()`→`redeemFloor`→成功显 explorer 链接+重载。**验证(preview 对 devnet 真链):** OneChain 有 redeemFloor/redeemQuote;`derive()` 的 vault `96vuVU…`/config `6nL6Mt…`/launch `DUu3bj…` 逐字节匹配记忆值,redeem_floor disc=`739e8a0d4833d83f`;**`redeemQuote` 对真 mint `9foiwALj…` 读回 vault=0.0997◎(=记忆里那笔 proven buy 灌的地板)、supply 164.9M、deployer 持 100%→est≈0.0997◎,ATA/金库/供应量读取全对**;coin 页赎回面板正确渲染、0 console error。**唯一没法 headless 验的=真 Phantom signAndSendTransaction(同 create 的已知限制,account 顺序是从已部署 struct 逐字搬)。** 为方便实测,给真 mint `9foiwALj…` 建了个 off-chain launch 记录(symbol AATEST,onchain:true)当 fixture——deployer 钱包持 100%,可用真 Phantom 在 devnet 跑通一次真实赎回 0.0997◎。**残留:** 赎回按钮的 i18n(现中英混排硬编码);赎回预估用 vault 全额(含少量 rent 储备,标了"≈");sell_market 网页客户端仍缺(赎回有了,卖出还没搬)。

**🔑 账号↔发币流程定案 = Model A「账号优先」(user 确认 2026-06-29):** 核心=**人本身是原创 IP,只有一个账号(在 oneIP.ai 邮箱注册出的 handle),它自动驱动 oneIP.io 的代币;.io 永远不是创作者正门,只是该账号的代币引擎/交易场;handle↔wallet 已互通=两边同一身份,绝不两边各建账号。** 明确否决 Model B「先在 .io 发币→再发原创」(=pump.fun 坑:币先来吸引投机客、币下无价值→归零)。规范流程 6 步:①.ai 注册(零币字样)②发布原创(实物/照片/视频/作品/打赏)③首次发布后台自动开"价值池"(占位、链下 escrow)——要**温柔露出**别静默 ④粉丝买货/打赏→20%净利进 escrow(情绪→金融价值就在此)⑤池子有真实热度/创作者主动→引导「激活价值池」=绑 Phantom+真发币(跳 .io /launch?account=,escrow 一次性 flush 进真实地板)⑥币在 .io 活,.ai 只留「价值池→」深链。**✅ 已实现并 preview 验证(2026-06-29）:** studio.html `renderVpool` 改成 4 态状态机:态0 无池→"发布第一个作品价值池自动开启";态1 池已开无 escrow→"🌱 你的价值池已开启·粉丝每次支持都在累积价值";**态2 有 escrow→醒目里程碑:显示 $X held + 渐变「激活上链 →」按钮(href=/launch?account=<handle> 带账号,保证 escrow flush)+ 说明**;态3 真链上→显示 floor SOL+成交数+币页链接。新增 i18n keys vp.firstpub/opened/opened_sub/held/activate/activate_sub(en+zh)。launch.html 成功框加**堵孤儿漏洞**:有 account(从 studio 来)且 migrated.flushed>0→"已把托管N笔销售价值灌进真实地板";**无 account(钱包直接来 .io 发币)→提示"给这枚币灌真实现金流,去 oneIP.ai 开店 →"(链 /studio)**,防止"只有币没有 .ai 店"的孤儿。验证:preview 实测 handle 注册→发数字品→态1"价值池已开启"、跑一单 settle escrow $9.65→态2 显示"$9.65 held·Activate on-chain→"按钮 href 带 ?account;/launch 加 nudge 后 0 console error。**都是要干的清单顺序(user 确认 1→5):1 真钱包实测赎回(user 侧,deployer 钱包+AATEST fixture,唯一没 headless 验的环,⏳ 等 user 跑)→ 2 流程 A/B/C(✅)→ 3 admin.html 分账 UI(✅)→ 4 buy/sell/redeem 网页三件套(✅)→ 5 清测试数据(✅)。** **✅ 4 交易三件套 chain.js 完成(2026-06-29, disc 逐字节对 Node 验证):** chain.js 加 `buy({mint,creator,solAmount})`(建 ATA if missing[createAtaIx idempotent data=[1]]+Buy 指令,账户序 launch/mint/vault/buyerToken/buyer/feeRecipient/creator/token/system)+ `sellMarket({mint,creator,rawAmount?})`(默认全仓,SellMarket 序 launch/mint/vault/sellerToken/seller/feeRecipient/creator/token)+ `borshU64`/`ataFor`(标准 ATA 派生,ASSOCIATED_TOKEN_PROGRAM_ID=ATokenGPv…);creator=launch 记录里的链上 creator(coin 页从 c.creator 传);feeRecipient=FEE_RECIPIENT 常量。coin.html header 加「交易 Trade」面板(仅 onchain 币):买入 SOL 输入+Buy、Sell all 按钮 + `COIN_CREATOR`(loadHeader set c.creator)+ `doBuy/doSell` 处理器。**验证(preview+Node cross-check):browser WebCrypto 算的 disc 与 Node crypto 逐字节一致:buy=66063d1201daebea / sell_market=e4b6b03eef8b4b03 / redeem_floor=739e8a0d4833d83f / create_token=5434cce4188cea4b;coin 页交易面板+买卖按钮渲染、OneChain 有 buy/sellMarket、0 console error。真 Phantom 签名同 create 限制未 headless 验。** **✅ 5 清测试数据完成:** 写了 clean.cjs 精确删本会话测试数据(判据=account.email 结尾 @t.co,正好 8 个)——删 8 账号/6 代理(agents.json 清空,本会话新建无真实伙伴)/4 产品/4 订单/2 buyback 币/4 launch/8 creator 档;**AATEST fixture 保留**(deployer 持 100%,留作真钱包赎回实测)。**⚠️ 还有更早会话的测试残留没动(非本次创建,按"没创建的别删"原则留给 user 定):accounts 里 Dbg97324/Reg27910/Unifier7508/PubUi70280/PubReady9378、products 里 TestCreator/StudioDemo/NewbieCreator*/FlushTest*/StudioAuto1、demo 的 AuroraTV/NovaPlays/GamerNova/LunaCreates。要清可再说。** **✅ 3 admin 分账 UI 已建并 preview 验证(2026-06-29):** admin.html overview 加卡片(Platform fee pool/Platform net keeps/Partner payouts/Referral payouts/Partners);新「Channel partners & payouts」section=注册 country/local 伙伴表单(local 需填 parent country id)+ 指派 creator→local 伙伴表单(country 自动派生)+ 伙伴列表表 + payouts 台账(Pool collected/platform base/各 partner 欠款/各 referrer 欠款)。JS loadAgents/addAgent/assignAgent/loadPayouts,接已有 admin 路由。验证:preview 登录(token Qwer1234@admin,注 preview 走 npm run serve 加载 .env)→ 13 张 overview 卡、6 个伙伴渲染、payouts 显示 Pool $7.25/base $4.83/partners $0.97+$0.48/referrer $0.97,0 console error。 **⚠️ 本次 preview 又写了测试数据(flow* 账号/产品/订单/buyback、AATEST launch 记录)到 data/*.json,清理时一并处理。**

**⛓️ 选链定案(user 讨论后拍板 2026-06-29):先 Solana(跟 pump.fun 一样),不做多链。** 讨论过要不要上 BNB Chain(BSC/PancakeSwap):技术上能(链下引擎链无关、`floorpad` 是现成 Solidity 起点可作 EVM 底子),但决定**先单链跑通**——理由=核心玩法是粉丝高频微额($0.5/$1 表情/打赏),**Solana 近零手续费+秒级确认是命根子**,BSC gas 会吃掉小额;且现阶段(devnet、未审计、小团队)双链=把最难最贵最危险的链上/审计成本翻倍、流动性劈两半,PMF 前不值得。**BSC 作为未来扩张第二腿保留**:架构保持链无关(buyback.js 按币的 chain 选适配器),将来加链=加个 adapter 文件而非重写;floorpad(Hardhat/Solidity,20%地板,6测试过)是 EVM 起点,但需补齐到 kolpad 现有机制(2%卖出税/redeem_floor/毕业)。**BSC 的真诱因=币安生态+东南亚散户基数(与 SEA 定位搭)**,若哪天 go-to-market 第一锤砸币安/PancakeSwap 叙事可重议。**⚠️ user 强调两遍的硬约束:"不能锁死"——继续 SOL 的同时,任何新代码都别写死 Solana 假设(coin 该带 `chain` 字段、链上动作走 adapter 接口、别把 solana-only 逻辑焊进上层)。选链的费用理由(pitch 用):粉丝高频微额 $0.5/$1,Solana 手续费~$0.0007(占 0.14%)vs BSC ~$0.1-0.3(占 20-60%)vs ETH >打赏本身——微额情绪金融化只有近零手续费链能承载。**

**✅ 未完善审计 + A/B 首批修复(2026-06-29):** 做了一次全代码 grep 审计(TODO/mock/stub/占位/dead code),产出验证过的"未完善"清单(见下)。**A 快赢已做:**(1)删死代码——ai.html 的旧打赏弹窗 `openTip/selTip/payTip`(476-512)彻底移除(早被 reaction 系统取代、无调用);(2)**BNB 留口子**——`launchpad.create` 加 `chain` 字段默认 `"sol"`(user 硬约束"不能锁死";将来加 BNB=按 coin.chain 路由 adapter,非重写)。**B 服务端 token 鉴权已做并 HTTP+浏览器验证:** 新 `accounts.verifyActor(token,claimedHandle)`=**已注册账号的写操作必须带匹配 session token(防冒充),未注册 handle/钱包保持开放(向后兼容,narrowing gap)**;server.js 加 `actorToken(req,body)`(读 Authorization: Bearer / x-oneip-token / body.token)+ `mayActAs(req,handle,body)`,接进 4 条创作者写路由:`POST /api/creator`、`/api/shop/product`、`/api/shop/product/active`、`/api/publish/now`(不匹配→401)。studio.html 加 `AH()` header 助手(带 token),saveProfile/publishItem/publishNow/toggleActive 全改用 AH()。验证:HTTP 4 例全对(带对 token→OK、无 token/错 token 冒充注册账号→401、未注册 handle→OK 向后兼容)+ 浏览器用 studio 的 AH() 模式发 /api/creator→"OK profile saved"。**注意:fan 动作(buy/tip/react)不声称"是创作者"、不受影响。** **B 残留(下一批):brand/link 路由未 guard;邮箱无验证邮件(注册即用);feed 钱包写(createPost)仍开放(钱包签名鉴权是另一套更难的活)。** **✅ 忘记密码/重置已建(2026-06-29, HTTP+浏览器验证全过)—— mock-first(同 Stripe/IPFS):** accounts.js 加 `resets`(token→{handle,expires},存进 accounts.json 的 `{accounts,sessions,resets}`,TTL `RESET_TTL_MIN` 默认30分)、`requestReset({id})`(id=邮箱或handle;**anti-enumeration:账号不存在也返回 {ok:true} 不泄露**;有 email 服务[`RESEND_API_KEY`/`SMTP_URL`]则 emailed、否则 dev 返回 `devToken` 让本地可测;每次请求作废该账号旧 reset token)、`resetPassword({token,password})`(校验 token+过期→改 salt/hash→删该 token→**作废该账号所有旧 session**→发新 session 即登录)。server.js 加 `POST /api/account/forgot`(总 200)+ `/api/account/reset`。studio.html 登录态显「忘记密码?」→ `forgotPw()`(读邮箱→forgot→dev 显 `?reset=<token>` 链接/prod 显"已发送")；`?reset=<token>` URL → `enterResetMode()`(隐藏 seg/handle/email,标题"重置你的密码",按钮"设置新密码")→ `doReset()`→改密+登录+清 URL;i18n si.forgot/forgotned/resetdev/resetlink/resetsent/newpw/setpw/resettitle(en+zh)。**验证:HTTP 6 例(forgot→devToken、不存在邮箱→无 devToken 防枚举、reset→新session、旧密码失效、新密码 ok、重用 token→失效)+ 浏览器(登录态显忘记链接、forgot 出 dev 重置链接、?reset= 表单变身、设新密码→进 dashboard+清 URL),0 console error。真发邮件待接 email 服务(env 已留 RESEND_API_KEY/SMTP_URL 钩子)。**

**📋 未完善清单(2026-06-29 审计,验证过)——剩下的:** 🟢**我能做**:home/.ai 全 7 语言(现 body 只 en/zh)、`.io` feed 帖挂表情(现仅 .ai)、退款窗口到期自动 settle scheduler、`.io` 0.75% 交易费四方分账走链下 Helius(现仅 .ai 那套)。🟡**等 user(钱/凭证/部署)**:Stripe 真上线(dev 即时付→真 key+webhook,表情/打赏计数挪 webhook,[server.js] TODO)、IPFS pin 线上验证(sandbox 网络不通,user 环境可)、14 平台发布真上线(VPS 部署 AiToEarn+key)、社交 OAuth 真凭证、真实货源/代发 adapter([source.js] throw not-implemented)、fiat→SOL 真兑换管道。🟠**链上 devnet→主网**:主网部署、真钱包实测买/卖/赎回(⏳ user)、Raydium 毕业真 CPI(现 stub)、Jupiter Verify、Flap 步④(revoke mint+immutable metadata,代码+编译好未部署)、escrow flush 真模式(现 mock re-key,[buyback.js:98])、安全审计(动真钱前必须)。

**✅ oneIP.ai 14平台发布 UI 补完(2026-06-29, preview+API 验证):** 复查发现 studio.html 的「一键发布到14平台」面板**结构是全的**(连接 chips `pubChips` + `pubConnect` OAuth 弹窗轮询 + `publishNow` 发全部 + composer/图片输入),**但只暴露了 6 个平台**(twitter/tiktok/youtube/instagram/facebook/threads),而后端 `src/publish.js` PLATFORMS 实际 ships **13 个**(+linkedin/pinterest/bilibili/douyin/kwai/rednote/wechat),且标题写死"14"(后端注释也写 14 但数组只有 13,标签陈旧)。**修复:** studio `PUB_PLATFORMS` 扩到全 13(带显示名 Twitter/X、抖音、快手、小红书RED、微信 等),标题改成**动态计数** `pub.title="…{n}…"`,`loadPublish` 用 `PUB_PLATFORMS.length` 填(且 setLang 也调 loadPublish,切语言标题不失效)——永远和列表一致,不再陈旧。**验证:** preview 登录后面板渲染 13 个 chip + 标题"一键发布到 13 个平台";API 驱动 pubConnect/publishNow 的完整链路(connect→mock sessionId→status authorized→accounts bound twitter→publish OK items=1 queued)全绿。**注意:(1) window.open 的 OAuth 弹窗在 headless preview 打不开(真浏览器正常,真用户无碍);(2) 真发帖需 AiToEarn 引擎部署(VPS)+ AITOEARN_API_KEY(=deployment 待办);(3) "14"实为 13——若要凑够/扩平台,需确认 AiToEarn 支持后加进 publish.js PLATFORMS(如 telegram/reddit/snapchat)。** feed.html 里另有一套旧的 wallet-based 发布 UI(老 Creator Studio),.ai 的这套是 handle-based 主用。

**💠 代币/门槛定案(user 2026-06-29,已核对代码属实 4/5):** **(1) 变创作者/发帖/生成占位币 = 全免费、零钱包门槛**——`ensureValuePool`(server.js:103)+`launchpad.create` 只建占位记录、不收费不碰钱包 ✅代码属实。**(2) 0.1 SOL 只在真上链收**——`LAUNCH_FEE_LAMPORTS` 仅链上 `create_token`(lib.rs:96)收,占位币不收;真上链激活总成本 ~0.11-0.12◎(0.1 费+账户租金~0.01-0.02+手续费≈0)✅属实。**(3) 每个币无差别 10 亿标准公平盘**——lib.rs:50-53 实锤:`INIT_TOKENS=1e9`(1e15 raw @ 6 decimals)、`VIRTUAL_SOL=2◎`、`GRADUATE_LAMPORTS=85◎`毕业上 Raydium、supply 从 0 公平铸(零预挖,创作者不偷拿)、毕业永久 revoke 铸币权(供应锁死10亿)、无冻结权(非貔貅)、Metaplex 标准元数据全钱包一致 ✅属实。**(4) 独家 20% 地板金库 + 随时 redeem_floor(lib.rs:204)**——比 flap.sh/pump.fun 多这层"永不归零" ✅属实。**防刷不靠钱靠两道免费闸门:内容门槛 + 一账号一代币(ensureValuePool 幂等)。SOL 只在主动上链交易时收——契合"粉丝非币圈/邮箱优先/零门槛"铁律。** **⚠️(5) 内容门槛防刷 = 还没做(唯一 gap):** user 要求「一段文字(≥N字)+ 至少1个:产品/照片/文件/视频/产品链接」才算有效发布(才触发变创作者+发币),防止裸一行字刷号刷币。**现状 `social.createPost`(social.js:78-85)只校验 `!text && !image → "post is empty"`**=有文字或图片任一即过,一行字无附件照样发+触发发币,门槛未生效;且 post 只有 text/image/product 字段,**无 video/file 字段需先加**。createPost 报错串 "connect a wallet to post" 陈旧(身份已邮箱化,应改 "sign in")。**✅ 内容门槛 + video/file + 0.1◎明示 + GitHub 平台 已建并验证(2026-06-29, user 定 ≥20字、不达标直接挡下):** `social.createPost` 加门槛(`POST_MIN_CHARS`默认20):**非 synced 帖必须 `text.trim()≥20 字 且 至少1个附件(image/video/file/product)`,否则 throw**;加 `video`/`file` 字段;报错 "connect a wallet"→"sign in"。**server.js `/api/posts` 关键重排:先 `createPost`(校验)再 `ensureValuePool`——不达标的帖 throw 在前,绝不会误生成代币**(HTTP 验证:短帖/无附件被拒 且 creator coin=none 无币;达标帖 posted 且 became creator 有 Kol…币;video-only 也算数)。feed.html composer 加 postVideo/postFile 输入 + placeholder "(min 20 chars + one attachment)" + submitPost 送 video/file + 客户端同款预校验 alert。launch.html 明示 0.1◎:加 "0.1◎ launch fee" chip(tooltip 说明占位免费、仅上链收)+ note 改成"pay a 0.1 SOL launch fee + a little rent (~0.11◎ total)"。**GitHub 加成第14平台**(publish.js PLATFORMS + studio PUB_PLATFORMS,给程序员/builder;注:非 AiToEarn 原生,真发需单独 GitHub adapter[OAuth+gist/release/discussion],部署时接)。验证:preview studio 显 14 chip+"一键发布到14个平台"、GitHub 在列;launch 页 fee chip+note 都在;feed video/file 输入在;0 console error。**激活上链流程(user 问)= 已建+字节验证**(studio 激活CTA→/launch?account=→Phantom 铸币付0.1◎→activateRealToken flush escrow),**仅差真钱包 devnet 实测 + 主网**。

**🎨 oneIP.ai 改 Pinterest 瀑布流(user 定的首页方向 2026-06-29):** user 给 Pinterest 截图,要 .ai 首页=**瀑布流图墙,每卡=一个原创者的作品/产品,悬停显详情,保留付费表情,点进去=Pinterest 式详情页(大图左/评论区右/相关在下),且详情里原创者能跳去自己设置的社交平台**。发现靠外部平台(14平台广播),.ai 只做主场+互动+变现,不做算法推荐。**✅ 第1步 首页瀑布流已建并验证(2026-06-29):** ai.html `#grid` 从 4列 grid 改 CSS columns 瀑布流(`.masonry` 5列响应式);新 `loadShop` 合并 `/api/shop/products` + `/api/feed`(有 image/video 的帖=works)成 `PINS`(product/work 交替混排);新 `pinHtml`=图满版+`.kbadge`类型角标+`.pin-ov`悬停浮层(标题+价/Buy或💝react)+`.pin-foot`(创作者头像/名+reactionTrigger);tabs 加「作品/Works」(`tab.works` en+zh),`setFilter` 按 `ftype`(physical/digital/work)过滤;work 卡支持 `<video>`。保留 openBuy/reactionTrigger/openCreator(点卡暂开 openCreator 抽屉,第2步换成专属详情页)。**验证:preview 自然加载 17 pin(10实物+2作品+其余数字/legacy)、tabs 过滤正确、0 console error。** **⚠️ 修了一个 init bug:setLang(第592行 init 时调)里第589行还叫旧 `renderGrid()`(已renname成 renderMasonry)→ ReferenceError 令 init 在 loadShop 前中断(表现:自然加载 0 pin、卡在"加载商品中…",但手动 loadShop() 有 17)→已改 renderMasonry。教训:renname 函数要 grep 所有调用点(尤其 setLang 这种 init 路径)。** **✅ 第2步 Pinterest 详情页 + 社交跳转 + 身份归一 已建并验证(2026-06-29):** ai.html 加全屏详情浮层 `#detailOv`(`.dt-ov/.dt-card/.dt-main` 大图左/信息右 + `.dt-related` 相关在下);点 pin 从 openCreator 抽屉改成 `openDetail(kind,id)`:大图/视频左;右栏=创作者(头像+**显示名**+粉丝,点头像仍可开抽屉)+ 标题 + 买/解锁按钮(product)或 💝react(work)+ **付费表情条**(reactionTrigger)+ **创作者社交平台跳转**(从 launch 记录的 twitter/website/telegram 生成外链,`.dt-soc` 胶囊,无则隐藏)+ 「价值池 →」深链 + **评论区**(work:GET /api/comments?post= 列表 + 输入框 `dtAddComment` POST /api/comments,fan=localStorage oneip_creator||guest);下方**相关作品**(同创作者其余 pin，masonry)。`closeDetail`/backdrop 关闭。i18n detail.socials/more/comments/nocomments/comment/send(en+zh)。**身份归一**:`/api/shop/products` 路由给每个产品加 `creatorName`(=social.getCreator(creator).displayName||creator),product pin 用它;work pin 用 feed 的 creatorName。**验证(preview):** 点 product pin→详情开(大图+名 Aurora TV/LunaCreates/GamerNova 真名+买按钮+表情),AuroraTV 多品→相关=3,建了个 work 帖→work 详情(图+名+表情+评论框,加评论成功回显),关闭正常,0 console error;footer 名字显真名(无 profile 的 legacy 钱包才显原串)。**socials 对当前 demo 币(AATEST 无社交字段)为空→优雅隐藏,未报错;有 twitter/website/telegram 的 launch 会渲染外链。** **注:留了个 demo work 帖(AuroraTV "Behind the scenes…" + 一条测试评论 "gh… love this set")当图墙演示内容,可清。** **Pinterest 改版两步全完成:①瀑布流首页 ②详情页+社交+名字。** **✅ 又补完 3 项(2026-07-02, preview+HTTP 验证全过):** **(1) 真图片上传**:server.js 加 `POST /api/upload`(零依赖,收 base64 data URL[png/jpg/webp/gif],≤5MB,存 `data/uploads/up_*.<ext>`,返回 `/uploads/` URL)+ `GET /uploads/*` 静态服务(仿 /assets/)+ import 加 writeFile/mkdir。feed.html composer 和 studio.html 产品表单各加 `<input type=file>` + `uploadImg(inp,targetId)`(FileReader→dataURL→/api/upload→填回 URL 输入框)。HTTP 验证:上传1x1png→/uploads/…png→取回 200 image/png、非图/坏 body 被拒;UI:两页 file input + uploadImg 函数都在。**(2) work 帖挂商品/币转化卡**:feed() 本就返回 post 的 `product`(productCard)/`coin`(coinCard);loadShop workPins 加 `product:x.product`;pin 卡 work 有 product 时右上角加 🛍️ 徽章;openDetail work 详情加「买同款/Shop this」转化卡(`.dt-buycard` 缩略图+标题+价+立即购买→,点击 openBuy);coin 的价值池链已有。验证:建带 product 的 work 帖→pin 显 🛍️、详情显"🛍️ tmp $10.00 立即购买 →"可点 openBuy。**(3) 全站发现 feed**:确认瀑布流首页本身就是全站聚合(所有创作者的 products+works 混排、works 按最新在前),无需另建独立 feed。0 console error。**注意:preview server 本会话多次重启后不稳(一次 upload 请求后自己退出,但手动 `node src/server.js` 完全正常 http 200)——是预览工具毛病非代码问题。留了 2 个 AuroraTV demo work 帖(含挂 product 的)当图墙演示,data/uploads 测试图已清。** **后续可选:真上传也可选走 IPFS(Pinata,已有 ipfs.js);work 帖挂币转化卡的独立卡(现只价值池链);图片压缩。**

**🧩 corax AI 网关接口已备好但按 user 指令暂停(2026-06-29):** user 给 `gateway.corax.live`(OpenAI 兼容 LLM 网关,26 个文本模型 gpt-5.x/claude-*/deepseek/gemini,**只文本无出图**)让"没作品的创作者 AI 生成素材发布"。**user 后来说"先忽略,我会给指令,不需要现在做"**——所以只建了接口、没接 UI、不再往前:新 `src/aigen.js`(零依赖 fetch,OpenAI 兼容 `/v1/chat/completions`,Bearer `CORAX_API_KEY`,`CORAX_BASE_URL` 默认 https://gateway.corax.live/v1,`CORAX_MODEL` 默认 gpt-4o-mini,kind∈post/product/idea,**mock-first** 无 key 返回 demo)+ server.js 路由 `POST /api/ai/generate`(free)。**顺手修了个 Pro-gating 残留 bug:`/api/ai/draft` 之前还 `402 "Pro ($10/mo) required"`(Pro 已砍)→ 改成 free + 优先走 corax(aigen)→ 回退 Claude(ai.js)→ mock。** .env.example 加 CORAX_* 注释。**待 user 指令再接 UI/真 key。**

**✅ 创作者「我的社交账号」绑定后台 升级完成(2026-07-02, HTTP+preview 验证全过）—— user 强调"必须方便每个创作者,不然没人来":** 把 studio 那个简单连接 chips 面板升级成完整的账号管理后台。后端:`GET /api/publish/accounts` 现在同时返回 `{accounts(publishAccounts),socials}`;新 `POST /api/publish/disconnect`{account,platform}(mayActAs 守卫,删 publishAccounts[platform])+ `POST /api/publish/handle`{account,platform,handle}(mayActAs 守卫,set/clear creator.socials[platform] 公开@账号);`/api/creator` GET 本就 spread creator→自动带 socials/publishAccounts。studio.html:`#pubChips`→`#pubAccounts` 账号行列表(`.pubrow`,14 行),每行=平台名+状态徽章(已连接/未连接)+**公开@账号输入框**(onchange→pubSaveHandle)+ 连接/解绑按钮;`loadPublish` 重写渲染行,`pubDisconnect`/`pubSaveHandle`(带 AH() token);i18n pub.connect/disconnect/connected/notconnected/disconnected(en+zh)。**.ai 详情页社交链改为读 creator.socials**(而非之前的 launch 记录)：`SU` 平台URL构造器(twitter→x.com/、tiktok→tiktok.com/@、youtube/ig/fb/linkedin/pinterest/threads/bilibili/抖音/快手/小红书/github 各自主页格式,wechat 无URL只显handle)+ `SL` 显示名;从 `info.socials` 生成外链胶囊。**验证:HTTP——set handle 带token→socials{twitter:@auroratv}、/api/creator 返回 socials、无token→401、connect tiktok→disconnect→none;preview——studio 14 行(平台+状态+连接钮+@输入,twitter 行预填 @socdemo)、.ai 详情页显真名 Soc Demo + 社交链 Twitter/X→x.com/socdemo、Instagram→instagram.com/socdemo.ig,0 console error。** **区分清楚:发布连接(OAuth accountId,一次绑定→一键14发)vs 公开@账号(粉丝跳主页)——两者都在这一个后台管;真发帖仍需部署 AiToEarn。** feed 页加了返回按钮(`.backlink` 纯文字风格同 launch 页)。

Distinct from [[club-poker-tycoon-direction]] — user explicitly said don't combine them.
