{
  "name": "oneip-io-launchpad",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "oneIP.io — kolpad: a Solana-style meme-coin launchpad (bonding curve, logo->IPFS, graduation).",
  "scripts": {
    "start": "node src/server.js",
    "serve": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
